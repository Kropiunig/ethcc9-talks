---
title: "Deep dive into Lean Ethereum - Spec & Implementation"
speaker: "Gajinder Singh"
organization: "Zeam"
stage: "Burton"
youtube: "https://www.youtube.com/watch?v=CNeUJGGhtFg"
---

# Deep dive into Lean Ethereum - Spec & Implementation
**Gajinder Singh** (Zeam) --- Burton Stage

Hello, hello. Welcome everyone. So this talk is about Lean consensus, spec and implementation, and I'm Gajendra Singh. I run Zim. So we'll look again a little bit of piling process. We'll do some pilling. Why lean? We'll then look at the work that we have done so far. Then we'll take a deeper dive into the components that make up lean, and then we'll look at Road 26 plan. Again, a bit of shilling. We are a global team running out of India, and we are on a mission, and the mission is Lean Ethereum, and we are fighting on all cylinders. So now looking at the straw map and coming to the topic. So if you look at L-star hard fork, this is where we want to get to post quantum security. We have post quantum attestations. We have lean VM aggregation. We want to reset some tech debt, and we want to start introducing endgame finality over here. But that doesn't stop over here. Then after that, we want to do one-slot finality, and we want to go to one million attestations per slot. And then we also want to take the post quantum work benefits to the other layers, data layer and the execution layer. Even though execution layer is a bit tangent to lean Ethereum, but things basically tie up. But things also start quite early because we will have post quantum pubkey registry very soon, and then we will also have post quantum heartbeat that will help us in finality and consensus. So now coming to Lean Ethereum, so it is Ethereum on Zika rails. And as we know, we have three layers. We have consensus, data execution, and what we want is post quantum security. We want verifiability that you don't have anyone in the world with a very low power mobile device can verify the chain. And we want censorship resistance. And that's the base setting. Now, what do we want to do on top? We want to be internet's settlement layer. So for that, we need 10K transactions per second on the execution layer itself, and we want 10 million transactions per second in the entire L2 ecosystem. So that basically corresponds to 1 gigagas per second on execution layer and 1 teragas per second on the data layer. And we want faster finality and inclusion. And on the consensus layer, we want 1 million attestations per slot. And all the benefits that we basically get for post quantum security on the consensus layer, we want to take it to other layers. And this is sort of a value proposition diff between the current chain and the lean chain. So we basically want post quantum, again, rehashing what we already talked about, fast finality, censorship resistance. We want to reduce the node complexity. We want to make protocol simpler. This is trust and verifiable complexity, which we want low, not just trust slash verifiability. Trust slash verifiable complexity is low. That means that the trust and verifiability should be high. And we also want decentralized block building. I mean, even today, in theoretically, it is high, but we only have very few builders. And that is a property that, okay, we are saying that we can compromise a bit on it. So coming to lean components, so there are three pillars that make up lean. Post quantum signatures and snarks around them, and fast consensus, and optimizations and improvements on P2P network. And when we are doing all those things, we'll basically, it will affect the entire way the consensus runs. So there will be a tech debt reset. We want to make the protocol simpler. And coming to what we have done so far, so we have done Devnet 0, which was just a start between three clients. It was just a warm-up using 3SF Mini. Then in Devnet 1, post quantum XMSS signatures came in. We jumped to five clients. Then we did SIG aggregation in Devnet 2. At Devnet 3, we introduced aggregators role, and we are 10 plus clients. But okay, some of those clients are white-coded AI, but... And our spec work for Devnet 4 is complete, basically, which is recursive aggregation across the attestations that are there so that the block size only has, block size can be really low. So we don't want the attestations which have same data to have different, which are still across the subnets, to basically, you know, be packed differently. And in Devnet 5, what we are planning is recursive aggregation across block, which basically means that the block will have just a single signature. There won't be many signatures. For example, even today, if you see, if you look at the block, attestations have their own signatures, and then there is a block signature and whatever. If there are other signatures, if there are other objects packed. So now in Devnet 5, we'll just move to one single signature for the block. And Devnet 6 and 7 is something that we are yet to decide, but I'll talk about it towards the end. Again, now let's look at post-quantum. So for post-quantum, we have selected XMSSSC, which is a one-time signature scheme. And what really happens is that you have these one-time keys, so each of these are one-time keys, and you basically drive a public key for all of these keys. You basically generate a sequence of these keys till whatever you think is the lifetime of your validator. You get to the leaves of this tree, and then you do a Merkel tree, and basically the root of this Merkel tree is your public key for a validator. And verification and aggregation of this will happen through SNARK. This is hash-based signatures. So BLS keys have, are more elegant in terms of aggregation and verification, but that is not the case over here. And that is why we will basically do them through LeanVM, something on LeanVM that we'll talk about that. One important thing to mention is that this signature scheme has been improved by some optimizations, and there is a paper, Top of the Hypercube, if you want to look deeper into it, to make sure that we get a very secure post-quantum scheme. And coming to the aggregation part, so LeanVM is the VM that will aggregate this, that is actually aggregating all the signatures. It is ultra-risk 5 that there are only 5 instructions in the set, and we are using Poseidon 1. Earlier, we were using Poseidon 2, but we shifted it back. And we are basically doing recursive aggregation on top of it. And the aim is to have this LeanVM formally verified, and we don't want to basically say that, okay, you know, if you are doing aggregation, you need GPUs. If you are an aggregator, you need GPUs to run. So CPU is still the target, and we want the final size to be produced so that we can, to be less than 1.5 KB so that, you know, we can just use a single packet to transmit them. And once we do all this for consensus, we take this to data and execution. So looking at consensus, currently we have the consensus on beacon, which is basically Casper. There is LMD ghost running, and then there is a finality gadget. But the problem with this is that it is tightly coupled, causing some of the issues and some of the ways we can't reason about edge cases. And it basically runs on epochs. So if we want to do faster finality, we can't have epochs anymore. So here we basically want a two-layer fully decoupled consensus protocol, and this image is from Jan's presentation, but attribution will come later. So what is happening is that you want an available protocol, and then you want to run a finality gadget on top of it. So you want fast available chain and decoupled finality mechanism with substantial stake security. And what about 3SF, what we are currently using? So it has fast confirmation and K-deep confirmation, and it has 67% stake-based finalization mechanism, but it is still coupled with block production, and we want to do one-slot finality, so it's not good enough. And here comes the attribution to Jan. And so what we actually want to do,


is we want an availability protocol called Goldfish, which is basically a heartbeat of randomly selected 512 validators who will basically do this heartbeat. And those are selected every slot. And then we have a finality gadget running on top of it. So Goldfish will be used to select the available chain. And finally, the votes will basically be counted and the finality gadget will give us finalization. Yep. So this is the explanation of how Goldfish works. And I suggest you look at Ian's presentation for further getting deep down into how consensus is going to evolve. And this is basically the basis for PQ heartbeat that we talked about in the roadmap. And it doesn't need aggregation. So we don't really... So we want to eliminate the time between vote and fast confirm. We don't want to add an aggregation layer on top of it, but it's just 512 words, so that's fine. And then with LMD Ghost and finality gadget, you will have your consensus. Coming to B2B gossip. So let's look at the objects that are there in the lean consensus network. There is attestation and PQ heartbeat. Basically, the objects are similarly sized. So it's basically an XMSS signature, which is three kilobytes, but we can decouple it into two halves. You have... You sell the tree. So basically, if we send a signature separately, which is basically the leaf mapping onto... Yeah. So if we basically send some message when it's signed, it gets mapped onto these hash chains. And if you basically send that only and you decouple this tree and send it separately, basically, you can fit it in a single network packet, which is basically what we want to go for. And all these attestations, they are happening at their logical subnets. And what we wanna find is a fastest path to the aggregators. Coming to aggregates. So what aggregators are producing. So which basically are lean VM proof and they are roughly three kilobytes. And we wanna again target them to a proof of one kilobyte. And it is possible. Resources are working on it. And then we want that they are... So these aggregators, they are at minimum hops to every validator that is out there. And then we want... So then there are blobs, but blobs are already sort of being worked on the beacon layer and that is something that we'll piggyback from them. And on the blocks, again, there is a single aggregated proof for everything so that we minimize this signature data and eventually we want block in blobs. And this aggregated proof is basically 300 KB. When I talk about this aggregated proof, I not only mean the proof of signed attestations or the block signature. I also mean that the proof of the validity of the block, for example, which ZKVMs will verify and produce the proof. So that is why this is 300 KB because the entire proof will be of this particular... of this size, which will just validate your block and which will just not only validate execution, but also the consensus and all the objects in it. So again, the emphasis is trying to get to the single MTU unit so that we can use datagrams and quick. We are already using quick on the current L2P2P layer and should we transition to an app-aware ETHP2P for the reasons that we can pipeline traffic per duties. We can do some source-aware peering so that aggregator can be very much connected to most of the validators and we want to remove the next slot bidding. So if you don't know about next slot bidding, what happens is that gossip objects of the previous slot are still being distributed in the current slot. So that is what is next slot bidding. And we want R and LC type encoding for big objects like block and blobs. Eventually block will also come in blobs, which is the long-term plan. So in the end, everything is blobs. Now coming to our plan. So we basically are at DevNet 4 testing stage because the spec is done for that. And for DevNet 5, we are basically doing spec, looking at DevNet 6 and 7. We want to do the consensus protocol that we talked about and we want to start introducing the P2P layer optimizations. And so this 2026 goal is to run 10,000 validators in a long-running DevNet. And the stretch goal is to basically have a public testnet. But that also means that we'll need to integrate deposit and execution integration. So this is sort of a stretch goal. But once we have that, we'll be able to launch a public testnet where people can come in. They can start deposits. They can do deposits, get their validators activated, and just join in the network. Thank you. Questions? Yeah, please. So I'm not 100% sure if you have mentioned it in this presentation, but with current layer 2s of Ethereum, you have like zero-knowledge proof protocols, right? And they are all facing the challenge that they have very slow finality. How does lean Ethereum is going to tackle both problems of having zero-knowledge proof and fast finality? You should have come to my yesterday's presentation, which talked about native rollups, where I just basically discussed the problem over there. So when you talk about L2, so there are objects going into the L2, and the problem over there is that, for example, there is a block that is produced on L1, then it takes sequencer, L2 sequencer a bit of time to detect those logs and build a transaction that they commit to their L2. So this layer is still fast. They take 60 seconds. But the reverse layer is extremely poor because they batch for hours and then they generate batch proof for hours and then they wait for finality on L1 for which might take 10 minutes, 15 minutes, and then they release, for example, the money. And this is for non-optimistic rollups, right? Optimistic rollups will wait for seven days instead of finality for the fraud proof window to happen. So if you want synchronously composable rollups, basically you need to go to native rollups, which will happen, I think, around the same time in L-star. How do you do the signature aggregation? Is it the same as post-quantum or? Yeah, so basically there is lean VM and a smart post-quantum SNARK is generated. So if you want to get into details of lean VM, you can basically go into lean Ethereum GitHub organization on GitHub and you will get lean sig and then you will get lean multisig. Lean multisig is the one that is doing aggregation. So you can look deeply into it and check how it's being done. It's totally out there. Hi, I'm wondering if we use native rollup, of course, it will make the confirmation faster and actually give a better security guarantee. But would that also mean like validator on the Ethereum mainnet need to help to do much more like data availability samplings before? Because now some layer 2s, they can use other data service, right? But after the native rollup, actually all the data will be directly put into the blob and assume the validator will need to, when they re-execute the layer 2's state transfers, they also need to do the data availability sampling. Would that actually increase the cost? So data availability sampling is basically when you are posting your block, right? So you are posting your block because you want the data to be available. And that, so when I say we are posting your block, you are posting your L2 block, right? So you have created a batch and


you want when you post, when you are basically confirming that batch on L1, you want to spread that data out, that is data availability. At that point, you can still say that, okay, you know, I can use any DA layer, but if you want to finally settle, which is basically providing the proof of your L2 batch execution, right, you can still do it directly through this particular layer, right? You don't need data availability to post your proof. I mean, the proof itself is, like, could be 300 kilobytes, but if you just use call data, you can still do it. And when native rollups will come in, basically what will happen is again, the proofs will be validated at the layer level, and those again might flow as blobs. So all of those things will be optimized as such for native rollups, but if you are not a native rollup, you need to take care of all these things. All right, thank you.

