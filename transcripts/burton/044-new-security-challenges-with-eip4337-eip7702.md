---
title: "New Security Challenges with EIP4337 & EIP7702"
speaker: "AliceAndBob"
organization: "Certora"
stage: "Burton"
youtube: "https://www.youtube.com/watch?v=sAN4VCQEkRQ"
---

# New Security Challenges with EIP4337 & EIP7702
**AliceAndBob** (Certora) --- Burton Stage

Hello, can you hear me well? So not a common first slide, right? So I have three ideas when choosing this first slide. The first one was to get your focus, of course. The second one is maybe there is real money inside this private key. And the third one was to make you wonder about what if there is another way rather than using non-really secure private keys, but maybe using account abstraction, which is maybe the topic of this talk. So hello everyone, I'm Lilian. I'm a security engineer from our verification researcher at Sartawa. Today I'm going to talk about account abstraction, so EIP-427 and 7702. The question we're going to try to answer today is how to trust delegators, how to be sure that funds are safe when delegating to 7702, and flows are secure when using 427. So for those who don't know about 427 or 7702, let me give you a brief introduction before diving in. 427 introduced an ultimate pool for user operation, a bundle to package transactions, and introduce a singleton contract, no hard fork required, works today on any EVM chain. 7702, on the other side, temporarily converts a user signing account into a smart contract wallet for a duration of a transaction by introducing a new type of transaction, which is zero X zero four, and it requires a PECTI hard fork. The main idea is to enhance the user experience and security with permanently altering the account structure. So same address, but now programmable. And both of them expand the attack surfaces. Yeah, I forgot about that. Let's deep dive into the 427 architecture because it directly explains the attack surface. So first, we have a user creating a user operation. This is an absolutely transaction containing up to 15 fields, like users, senders, calldata, gas parameters, paymaster, if used, signature, of course. This user op goes into a user mempool. These are different from the Ethereum mempool we all know, and it's optional. It's more and more used, but it's still optional. And then we have the bundler, which are off-chain actors that collect the user operation, and as you may have guessed, creates a bundle transaction with the valid user ops. Then we have the entry points. The entry points orchestrates the simulation and execution in two phases. So first validation loop, check signature, check user paymaster funds, if used, and then the execution loops, which actually runs the operation. Finally, we have the postop, which is also optional, and this is where if used the bundler gets compensated by either the user or the paymaster. About the entry points, keep in mind that it's a single point of failure by itself. That's why it had multiple audits and has an active bug bounty and can prove. About 7702, the architecture is simpler, but the implications are more profound. You have your EOA, you sign an authorization tuple containing chain ID, address, nonce, signature, and then the EVM will write a special code to your EOA, which is zero X EF0100 plus the delegate address. And then when your EOA is triggered, the code will run in your EOA context using your storage, your balance. Two things to remind about 7702, for now, the first thing is that the delegation is revocable. You can delegate to address zero to clear the code. And you can still sign raw transactions for now. I mean by that that even when delegated to, Alice's private key can still sign raw transactions. So let me crystallize the key differences here in terms of layer. 427 is application layer, no hard fork required. 7702 is protocol layer. It requires a pictor hard fork in May 2025. In terms of account model, 427 introduced new smart account address, so migration required. 7702, on the other side, preserves the existing EOA address, so no migration required, which is huge for UX. In terms of infrastructure, 427 introduced bundlers, almempool, entry points, so external actors are required. And on the other side, 7702 use native transactions, so no extra actor required except delegator, but usually you is a trusted delegator or you develop it yourself. And finally, in terms of private key, in 427 it's even optional. You can have fully smart accounts. And 7702, as I said, always retain pseudo access. You can remove the root key control for now. And in practice, they are complementary. I mean by that you will see EOA delegating via 7702 to 427 compatible wallets, cards by using paymaster for gas sponsorships. This is the likely production pattern. So let's see some exploit, and then the idea is to map the attack surface. When we are talking about account abstraction, 427, usually it means Alice will pay gas for Bob. And two questions here that can come to mind. The first one is, what if Bob has the possibility to not refund Alice? And more than that, what if Bob has the possibility to drain Alice's ETH? Then, one thing to know about the 427 architecture is that the entry point will charge a 10% penalty on the unused gas exceeding 40k. And this penalty is deduced either from the user or the paymaster. So let's see a paymaster gas drain exploit. This is a simple grieving attack. So we have an attacker that creates a malicious user ops. We will see why malicious later, but let's say the total gas limit set by the user is 500. Then the paymaster agrees to sponsor it because why not? The entry point executes and the actual gas used is only 50k. So the entry point will say, okay, you asked for 500, I use 50k. It means that the penalty will be on 450k, it means the 40k buffer, which is equal to 41k gas of penalty. And here, two problems. The first one is, what if the paymaster forgot to account for the excess penalty gas cost? It means a user can create a malicious user ops by setting the call gas limit a lot higher, but if the paymaster forgot to include the potential penalty gas cost, the paymaster will pay for the excess. And the second problem, which is worse, you can design a malicious bundler that can be created to always trigger the penalty gas cost and the paymaster will gently give to the bundler to you the penalties applied by you. It's like applying taxes on a project your client are forced to buy. It might remind you someone. And the fix is easy here. You just have to account for the potential penalty gas cost. So let's see something similar in 7702. It's a more severe scenario. It's well-known, but it's a common example. So we have Alice. Alice that wants to use 7702 and wants to delegate to delegator A. Delegator A layout is simple. You have nonce in slot zero, you have inventory fixed, and you have balance in slot one. And let's say after some couple of transactions, Alice slot zero and nonces are set to six. And Alice was smart and sold her BTC at 120k, like everybody in this room, right? And she has 10 million USDC in slot one. Then Alice wants more fine-tuning, and she wants to delegate to delegator B. Delegator B layout is very different. We have an array of uint256 item in slot zero, and we have a mapping of data in slot one. And then when Alice EOA is triggered, delegator B will read the leftover states from delegator A and will set the item that's linked to six, which basically will break the account. So the logic is corrupt because we have an unpredictable behavior and it's pretty hard to fix because the storage layout is now unknown. So simple takeaway here, remind that the EOA storage passes across with delegation. So let's see another 427 example. One thing to know about the 427 type of paymaster is that there are basically two kinds of paymasters. The first one has the validation paymasters that are used mostly for gas sponsorships. So they are doing a lot of verification, validation to decide if a user transaction is eligible for gas sponsorships. And there are the ERC20 paymasters, the one we will focus on for this story. The ERC20 paymaster enable user transaction inclusion in exchange for ERC20 token equivalents. This kind of paymaster is charge user before the execution, so before the entry point or after the execution, after the post-op. So please raise your hand if you think that the paymaster should take the fund before. Okay, and raise your hand if you think that the paymaster should take the fund after. Okay, so a lot of... No. So let's see an example where the paymaster takes the fund after. And here, what do you think will happen if the user execute and revoke the approval as part of the execution phase? So it is...


this post-up will fail and the entire transaction will fail, which means that everything will fail, of course, but the payment master still has to pay for funds. So here, the good answer is to take the fund before, because we always favor the protocol, but there are a couple of problems we have to face. The first one is that we must estimate the gas upfront based on gas limit. We will end up overcharging the user because you can't predict the exact cost. You have to account for the 10% penalty and you have to not be a victim of all the oracle conversion risks that could happen. I mean by that DOS, decimal issues, and conversion price. So if you take the fund before, you will end up overcharging, which is a way to go, but if you take the fund after, there is a risk of undercharging the user or not charging it at all because the user can revoke the approval during the execution. So yeah, the golden rule still pre-collects the maximum fund as possible before, and if there is excess, you refund the user. It's easy. About that, there is an ERC that tries to mitigate the simulation execution gap we have in 427. This is the ERC7562. This ERC basically do two things. The first one is that it bans opcodes that anything that could change between simulation and execution are banned. So timestamp, block hash, base fee, Coinbase are banned, and it does one more thing. It restricts the external storage access rules. So basically, you can only read mappings where the sender is the key. So the takeaway for ERC7562 is that it reduces the attack surface, but doesn't eliminate the fundamental doctor problem we have in 427. And another edge case on 427 is that during simulation, the storage is still maintained cold, which could represent a difference between simulation and execution in terms of gas cost. So one thing to do about that is to have the entry point performing a view call before validation to kind of warm the storage slots. So this help reduce the discrepancy problem we have here, but keep in mind that if your validation success depends on simulation gas cost, I mean cold versus warm gas cost, you can still give the paymaster. So we have seen some attack. We have seen some defenses. Let's try to map the 427 pitfalls we have fundamentally. The first one is about the signature. This is where most implementation bugs live. So when you are discussing about signature for 427, you have to do two things. The first one is you have to include chain ID, entry point address plus the fees and gas parameter in the hash. This is really important. If you miss any of this, you enable replay attack. And second thing, there are the valid until and validator fields that exist, so please use them. You really have to treat the signature as a library decision and you have to bind the signature to the user hub with the hash for a user. This is really important. Second thing, you have to not revert on the bundler for invalid signature. Your bundler is an off-chain actor. So if you let someone spam your bundler, you enable DOS attack for it. So you have to let the entry point under the revert scenario, so your bundler must return the sig validation fail, never revert on the bundler side. Number three, this one is funny, but the original 427 missed it. You have to verify that the message sender is the entry point on your paymaster. Otherwise, it's open door. And finally, you have to account for the 10% penalty as we discussed. There are a lot more. It will be too long to discuss all of them, but you have to maintain your valid user op as stateless as possible. You have to never rely on post-up for core invariant and keep the post-up as minimal as possible too. We can discuss this later if you want. Let's do the same for 7702. Here, four well-known critical attack surfaces. The first one is the chain ID. If you set the chain ID to 0, it makes the transaction invalid on all EVM chains, so you don't want to do that for two reasons, because it's obvious, and the second one is because the same contract address on different chains may have completely different code. So please always specify explicit chain ID on iteration tuple. Second one is initialization front-running. Remind that the 7702 has no init code, so constructor don't run in your EOA context. So if you want to set role permission owner, you have to do it atomically, I mean delegation and initialization in the same transaction, and never delegate to proxies or upgradeable contracts, but only delegate to immutable on-objects contracts, because if your EOA is upgradeable, it's logic, but the guys that control the upgradeability controls your EOA. And remind that the same is true for 427. The attacker can extract the init code from a user op and front-run the deployment. Number three is access control and delegation. This is where the 0xef0100 belongs. So first, it's well known, but it has caused more than $3.2 million in losses, so I had to talk about it, but never no longer rely on take.origin. And if you want to detect delegation in your smart contract, you can use message.sender.code. Number four, we have talked about this, storage spaces when you redelegate to a new contract. So against that, we have two layers of defenses. The first one is to use 7201 namespace storage to derive the storage load in a unique way. You can follow ERC779, which is still a draft, I think, but it provides a way delegation standard for safe migration. And finally, it's not really handy, but you can clear the storage explicitly before any red delegation. I mean by that, you can do it by hand with Solidity or assembly code in your delegator contracts. So what's next for account abstraction security? There are two EIP worth knowing. Both are from Vitalik. You may know him. The first one is EIP8141, which is called anonymous frame transaction. The idea is to move 427 from application layer to protocol layer, so no more off-chain bundler, no more entry point middleman. And the idea is to do that by introducing a new type of transaction, zero X06. About 7702, there is this new EIP also, which is 7851, which aims to deactivate the private key override and delegated EOA. So the idea is to provide a new precompile to deactivate the private key and once disabled, the private key can only be re-enabled via the delegator itself or multi-sig. So, and about that, there is a follow-up EIP8151 for EC recovery because here the idea is to make EC recovery aware of the deactivated private key. Otherwise, the bypass is easy. And two more things worth knowing. The first one is ERC7739 already used by OpenZeppelin contracts and REVE7560, which aim to patch native Auctop abstraction by using two-dimensional nodes. The key takeaway from this slide is that account abstraction is not a finished feature and has an evolving surface attack. So what we discussed today may not be true in six or 12 months. Thank you very much. Account abstraction is a deep and technical topic. So if you need any help, a design review, manual auditing from our verification, or even web2 auditing service, please come to see us. I still believe that account abstraction is the only way to onboard the next wave of users. So if you want to dip more, please follow me or the Sartu account on X. Thank you. If there is a question, we have two minutes. Thank you. Hello. Yeah. There is a part I did not quite understand, is you say that you should never delegate to an upgradable contract, but my understanding is upgradable contracts, there are two parts. There is a proxy and the implementation. Actually, delegating to the implementation feels like what we should do because they are designed to be updated. They use namespace storage. And if you were to upgrade to a proxy, what the proxy does is it just re-delegates to another address that is in no storage, which in our case would be the storage of the EOA, so it would just basically not work. So what exactly is the danger here? I'm just trying to describe the right case. I wanted to keep things as simple as possible, but you're right. If you know what you are doing, I would say you can do this kind of thing. That's right. Hi. Of the two new EIPs, from a security perspective, which do you prefer? Both are targeting different things, but I think that moving account abstraction 427 from application layer to protocol layer is a really good thing to do because we have modules in 427. We have modules do a lot of things that can be fine-tuned, and this is where usually errors are found.


Thank you.

