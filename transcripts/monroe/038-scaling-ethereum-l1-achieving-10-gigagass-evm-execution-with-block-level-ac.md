---
title: "Scaling Ethereum L1: Achieving 10 GigaGas/s EVM Execution with Block-Level Ac"
speaker: "Qi Zhou"
organization: "QuarkChain"
stage: "Monroe"
youtube: "https://www.youtube.com/watch?v=a_t7VKufgqQ"
---

# Scaling Ethereum L1: Achieving 10 GigaGas/s EVM Execution with Block-Level Ac
**Qi Zhou** (QuarkChain) --- Monroe Stage

Hello everyone, it's my great pleasure to be here to present our latest work on Ethereum scaling, how to achieve 10 gigahertz per second EVM execution with block level access lists. So a quick intro of myself, my name is Chi. I've been working in this space for about eight years before I was a PhD and also engineer at Facebook and Google. I'm particularly interested in Ethereum infrastructure, especially scaling, and received a couple of support from Ethereum Foundation, including Vitalik personal grant. So why we want to do this work? So we know that Ethereum is aimed to scale dramatically by increasing the gas limit of Ethereum layer one. So the current goal is to target for 100x gas increase so that we can achieve about 5 gigahertz per block. So which means that in order to validate the blocks in about four seconds in the consensus time budget, we will require at least about 1.6 gigahertz per second of the execution throughput. However, this create a concern that whether we are able to handle this performance using current client, especially based on our commodity PC to ensure decentralization. So here is the number that these days we can get that now gas can only offer about 300 million gas per second, and also RAST is a little bit higher, close to 600, but still far from our target. So why the scaling is so difficult? The main reason is that there are a couple of the limits imposed, which both in terms of the disk IO and also execution, CPU execution latency. So here is one of my favorite diagram, which is generated by flashball a couple of years ago. So about this shows that the disk IO op codes like SLOAD and core op codes takes about 70% of the execution time without any optimization like XML prewarming or optimistic execution. So, and then block level access lists basically remove this bottleneck from disk IO to CPU execution. So Tony give a really excellent introduction of the block level access lists. So if you want to learn more, I will encourage you to watch his presentation yesterday. So basically the idea is to include what the transaction in a block will access, including its account and also storage smart contract, so that when we begin executing the transaction, we are able to preload all this data in parallel so that we can fully utilize the disk IO. So on the right hand side is a diagram shows the number of throughput versus the number of parallel IOs, where we can show that right now our disk can easily support more than 32 IOs concurrently so that we can easily achieve about 30X performance improvement in terms of disk IO. So that now we can remove those random disk access to latency now to the current sequential execution. So with all these ideas, now we are trying to study how BAL can also improve the parallelization in terms of CPU execution. So here is our setup. We are running all these experiments on top of Ethereum mainnet blocks so that we can get some real numbers. And we are also running on top of AMD 16-core CPUs, which takes about 100 euro per month to rent this machine. And also we are experimenting, so simplify the whole experiment so that everybody can easily reproduce our numbers. For example, all the states have been preloaded so that we don't need to worry about corresponding IO cost. And we also recover all the senders, which is very CPU intensive, but it's not related to EVM execution. And lastly, we do not include the state root computation, which can be amortized by the large blocks that Ethereum is growing. So the first number we get is to just run all this execution sequentially without any IO cost. And so this is the number that we can fully utilize a single CPU, 100% utilization, and the number we can get, which achieve about 1.2 gigahertz per second. And then we, by increasing multiple threads so that we can run corresponding transaction in parallel within a block, we found some interesting number. So we found when we increase the number of threads, we see some performance improvement, but it quickly capped at about four times performance data, about 5 gigahertz per second. The reason is that we found there are some of the transactions that can be hardly be optimized by parallelization. So there's a famous called Amdahl's Law, which tells that even though we are able to improve the, increase the throughput by parallelizing all these transactions, however, the whole execution time will be limit by the longest transaction that can be hardly be parallelized. So here is the famous equation that we can see this number called one minus time optimize. That is the part that accounting for the longest transaction. So in the Ethereum mainnet, we also do the experiment where we found that even we are able to increase the number of the threads to help to improve the total time, but the longest time the transaction execution latency just keeps constant and which result in four times performance cap in terms of the parallelization threads. So how we are able to solve this problem? The basic idea is how we are able to break the granularity so that we can have smaller granularity by two directions. One is to improve the block size, block limits, gas limit, which is Ethereum is currently doing. And second thing is to limit each transaction, transaction gas limit so that the ratio between these two will be small and which means that we are able to increase the parallelization even with multiple threads like 32 threads. And so this gives us a really encouraging numbers whereby we merging the mainnet 50 blocks, try to simulate the mainnet execution, but after that, after example, 50 or 100 gas limit increase by merging these blocks so that we are scheduling all this parallelization among these 50 blocks rather than single block, which we can show almost linear scaling in terms of number of threads. And the final number that we can achieve is about 40 gigahertz per second, which means that we are able to achieve not only 100X, but almost 1000X of performance improvement by increasing the gas limit of Ethereum. That is really, really encouraging number. Also, we do some study on how to, for example, changing the size of the mega blocks from one to the 200. And we found that right now, 50 megablock is the sweet point, which is pretty fit to the current Ethereum scaling plan to increase 100X block size, block gas limit. And so these numbers are really encouraging, but we still facing a couple of engineering challenges if we really wanted to put these numbers into the production. So here I'll list a couple of the challenges we found during our experiments. The first one is the core sender recovery. We found that the sender recovery is significantly underpriced. So even we are able to fully, for example, computing all this sender recovery, but accounting all this recovery computation speed, it just, the performance significantly reduced to about 5 gigahertz per second, which means that we need a better way to solve the sender recovery, either increase the gas or either we are able to use in other more powerful computation devices like GPU. And there's also other things like, for example, we do not consider IOs because like all the data has already preloaded into the memory, but actually the IO will also, even we are able to do it in parallel, but still take maybe takes considerable time. And also the BAL itself can take significant overhead in the block size. So we need advanced algorithms, for example, P2P algorithms or EPBS and also peer desk can also help to propagate those BAL information with, for example, pipeline execution so that we are able to fully utilize BAL without worry about the publication latency. And another thing is like the BAL here is more experiment for the validators, but we are there's also limits from the builders how it's able to build this BAL quickly. So this is the right now a really open questions. Maybe the idea is using super powerful builders and then using all in-memory database, all these kind of techniques. But still a lot of the, what's the performance limit is here is still a open question and probably with our future research work. And lastly, it's about how we are able to compute the state rule for this direction. I feel a bit more optimistic because we are able to amortize the cost with larger block gas limits. So here I give some quick results of how we are able to reduce the BAL size with the parallel IO considerations. So the basic idea is that actually we have different types of BALs with the larger size of BAL may contains more information. For example, it may contains information of the, not only the location that we are going to read, but also corresponding value. And also we have to include the post transaction deeds so that we are able to schedule the transaction execution early. So there are here we summarize three types of the BALs. We showed that the parallel IO BAL, which only includes the post transaction deeds, will reduce the corresponding BAL size to about one third with the most expensive full BAL, which contains all the information, IO informations during the execution. And so we try to understand suppose we are want to reduce the BAL size for example, to one third. What is the performance difference there? And our result shows that by reducing this, the BAL size, we are able to still achieve some pretty good numbers, especially I think that the numbers that should be enough for our midterm or near or midterm requirements on the user transactions. So it's about like achieve a 10 gigahertz per second, which is about six times of the even 100X of current gas gas limit. So here is the summary of the today's talk of BAL and by increasing the gas limit of Ethereum with by also imposing the per transaction gas limit, we are able to significantly improve EVM throughput with more than 10X or even more as demonstrated by our experiments. And also we are found some, there are some limitations during this study, like how we are able to improve the

