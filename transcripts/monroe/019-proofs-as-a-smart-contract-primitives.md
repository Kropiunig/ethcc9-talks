---
title: "Proofs as a smart contract primitives"
speaker: "Henri Lieutaud"
organization: "Starknet foundation"
stage: "Monroe"
youtube: "https://www.youtube.com/watch?v=wXbSkjmkQ5o"
---

# Proofs as a smart contract primitives
**Henri Lieutaud** (Starknet foundation) --- Monroe Stage

Hey, how's everyone? Who's sleeping? I mean, will you raise your hand if you're sleeping? I don't know. Who's not sleeping? Yay. So hello, everybody. Thank you for being here for my talk today. My name is Henri Liu-Tau. I work at the StarkNet Foundation where I'm VP Product. And today I will talk about ZK proofs as primitives, primitives for blockchain developers and primitives for smart contract developers in particular. So during my presentation, I will go through what's is here on this slide. So I will start with a small explainer, a refresher, if you will, of what proving is, how it is used today, right? Then I will explain how proving can be used by smart contract developers to come up with more interesting systems, more interesting protocols, and more interesting smart contracts. And then I'll explore a couple of use cases that can be useful, that have a potential of being somewhat interesting. And you'll tell me what you think. So I'll start with a short explainer of proofs, right? Who here feels comfortable with what zero-knowledge proofs or validity proofs are? Okay, who feels somewhat confident, I've heard about it, I know a little bit about it, but not too much. Okay, who has heard about proofs, but has no idea how they work, but, you know, they're a nice word, proofs. Come on, five letters, proofs. Okay, so people are fairly technical. You know what proofs are. So proving is a mathematical tool that enables a person, which we usually call the prover, to execute a program and prove to another person, who's called Bob, as often in crypto, and who's called the verifier also. And Alice gets to prove to Bob that she performed a specific computation without having to reveal to Bob what went into doing that computation, but also, importantly, without Bob having to do the computation himself. So it saves work on Bob, and it preserves information on the side of Alice. ZK proofs are a fundamental mathematical breakthrough. They're not specific to blockchains. They're mostly used in blockchains today, but they have a field of application that is much wider. So proving today is mostly used in prods in blockchain networks that are called ZK rollups. In a nutshell, a lot of blockchains today are faced with a scalability dilemma, right? Either if they want their network to be decentralized, they need every participant to replay everything that is happening on the network, right? Blockchains are often called distributed ledger when in reality they're not distributed, they're replicated. And so most networks have to find a balance between enabling enough people to participate in the network and having a lot of throughput. And ZK rollups enable to have sort of the best of both worlds because you can have more performant machines that operate the network and then generate a proof that they did so correctly. And now the participants in the network don't have to operate the network. They can just verify the proof that the network was indeed operated properly. To a degree, ZK proofs in ZK rollup enable to do to execution what mining and proof of work enables to do for security on proof of work networks, right? And so in ZK rollup, this is where most of what is being done today is done. I have a small lexicon for people who are a bit lost. Who knows? So as a lot of things in crypto, also, we're terrible at naming things. So we use the same name to distinguish between several entities. So the first I want to define is the prover software. Prover software is a computer software that you run on your machine. It allows you to run a computation and generate a proof that you did so properly. Then there is the prover, the entity. And here the prover is the person who runs the prover, right? So think of it if you think about an Ethereum node, that is the difference between a node and a node operator. Here you have the prover, the software, and the prover operator, the entity. But it's often called the prover. And we have the same distinction for the verifier. The verifier is the software that takes the output of the prover and then will give you, yes, this is a correct proof, or no, this is an incorrect proof. And so you have a software that is called the verifier. Interestingly, that verifier can be a smart contract. So you can write an Ethereum smart contract in Solidity that verifies ZK proofs. Then you have another entity, which is a software, an entity this time, which is a verifier that runs the verifier. It can be a person. It can be a machine. It can be a smart contract. It will eventually also be agents. And then finally, for ZK rollups, you have another entity, which is called the sequencer. The sequencer is the entity that runs a big machine, aggregates transactions, batches them in a block, and then gets into a relationship with the prover to prove that those blocks are correct. In a nutshell, that's how ZK rollups work. So far so good? Yes, I see thumbs up. Nice. All right, so now I want to talk a little bit about ZK proofs as primitives for smart contracts or ZK proofs as primitives. So the first thing that is important to understand and to note is that at this stage, most of proving in production is facing a trade-off. Writing a prover software is actually really difficult. It's not super common, right? So there are fairly specific programs. You will write a prover for a specific application, or you will have some provers that are general purpose. But the provers who are general purpose are in general harder to operate, and so you see mostly specialized actors using them. So you sort of have this dichotomy. If proving is done by smaller entities, then designing the prover is difficult, and you don't have many systems doing this. And if proving is general purpose, it will be mostly done by specialized entity that runs ZK rollup, right? So you see mostly this dichotomy. ZK rollups who are operating provers at scale, and then in specialized applications, you will have smaller provers and verifiers, but the applications are fairly narrow. They're not extremely complex. They don't allow for general computation. It is a little bit the same. You can use the same mental model where we are today as what computing would be in the 70s. I know it's not a perfect comparison, but bear with me. In the 70s, you could use computers at scale. You would have those big mainframes that would operate things for a lot of people, but it was not very common for people to have their own machine. It wasn't very common for people to run their own server. And if you had people doing some sort of compute on their own server or some smaller entities running their own servers, those servers would be, you know, specialized circuits or specialized machines. They wouldn't be general purpose. Now, protocol level proofs, as we see them, aim to flip that and enable a democratization of proving. They enable using the same prover for general computation, but at a level that is not for a big operator to run, but for smaller operators to run, maybe even individuals, right? So it's a little bit like informatics and personal computing in the 90s. You went from having a system with very big machines and very big operators to a system where there were a lot of smaller machines that in the beginning couldn't interact with each other, but eventually made it there, right? So why is this interesting? Proving in general is a really powerful primitive. You get to play with it for a bunch of different directions that I'm going to get into in a second. But the general gist of what this is, is we want to make proving a primitive that smart contract devs can use easily, very easily, to delegate part of their smart contracts to the outside for scalability purposes or for privacy purposes. So what does easy mean? It means that when you want to write a smart contract that leverages a proof, one, you don't have to learn a new language. You can use the same language as the one you're writing your smart contract in. Two, you can reason about your programming environment in the same way that you reason about your programming environment when you're writing a smart contract. So the concept of a transaction still is there. The concept of a block still is there. The concept of interacting with another smart contract is there. Same for account and state. So you get to write smart contracts that have proofs, that affect their state. And the same code that you can run on a public blockchain, you can also prove outside. That's a very powerful primitive, though very unexplored. So this is not just words. I have a small demo up, so please flash it. Kudos to Adrien, who has worked on this over the past week to make it happen. This is something that is coming to production on StarkNet in a couple of weeks, and you can already try it today on testnet. So this is not a fully fledged system, of course. It's a first version. We're developing it right now and we're rolling it out to see how people interact with it, to see what people will start building with it, and to see what interesting use cases may come out of it. Things that will come in the future will include having a much bigger proof size, so being able to prove much larger workloads from outside of your smart contract. We'll also enable more usage of advanced functionalities inside your proofs. You'll have full recursive verification. I'm not going to go into recursion now because I'm not going to go into recursion now. And then you can do transaction aggregation, hopefully, sometimes in the future. All right, so now let's look at potential use cases, right? What can you build with this exactly? So one thing, of course, you can do stuff on the side of privacy. Privacy is the original use

