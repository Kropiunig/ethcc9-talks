---
title: "Ladislaus von Daniels (Ethereum Foundation) ZK Attesting"
speaker: ""
organization: ""
stage: "Hepburn"
youtube: "https://www.youtube.com/watch?v=0-rScovCvNk"
---

# Ladislaus von Daniels (Ethereum Foundation) ZK Attesting

Perfect. Good morning, everybody. My name is Ladislaus. I'm part of the EF protocol coordination team. And yeah, today I'm going to talk about the ZK-EVM on L1 and ZK attesting corresponding to it. And so the basic problem we are trying to solve here is that we want to scale Ethereum. We want to, like, scale it to global demand while at the same time preserve the guarantees we have today, which is particularly an uncorrelated validator set. So the two North Star technologies we have identified on the roadmap for many years are on the one hand data availability sampling, and on the other hand zero knowledge technology. And as you can see on the slide, ZK appears twice. So I will focus on the ZK part of it, and particularly in the context of staking. So just to quickly reiterate, zero knowledge proofs, many of you have heard of in the context of ZK rollups over the past five years, the technology has matured along the way, and we currently feel sort of confident enough to consider it for the L1 itself. And the irony is sort of the rollup-centric roadmap in some sense is perfectly on track as we're trying to make the L1 a rollup itself. And so, yeah, what this means for nodes essentially is that instead of every node on the network re-executing all of the transactions in a block, they will instead verify these ZK-EVM proofs. And yeah, this is one of the, probably one of the biggest architectural shifts in the history of the Ethereum protocol. It touches the consensus layer, the execution layer, it touches cryptography, networking optimizations, the economics to it, governance, if you will. We introduce ZK-EVM vendors, rollups are affected, client teams. This is a big shift. Why would we do this? Like, what are the special properties of these ZK-EVM proofs? ZK-EVM proofs are extremely efficient and like by the virtue of the technology, if you will. And what we are particularly leveraging here is sort of compute compression. So verifying these proofs is extremely resource. It's not resource intensive. So you think of it as verifying these proofs on like smartphone type devices. And the beauty is that we can do this consistently fast. So regardless of the block size, we can verify these proofs extremely cheaply. And so what we effectively get is the standardization of the cost of block processing, which is a beautiful property. And as Carlos has alluded to in the previous talk, we introduce this notion of statelessness with ZK-EVM proofs as sort of the, by verifying the block, the ZK-EVM proofs, we can guarantee that the block is valid. And so there's no need for us to hold the state anymore. And for all of the newers in this regard, check out Carlos's talk, the previous talk. So why would we do this? Like, what are the big benefits of gas? First of all, users benefit. We can drastically increase, it allows us to drastically increase the gas limit, which is beneficial for all of us in terms of higher throughput, allowing for more economic activity happening on Ethereum, obviously also cheaper fees. Rollups benefit significantly from this as sort of, they now are inheriting sort of this ZK mainnet grade technology from the L1 itself. And validators, first and foremost. So these ZK attesters, we lower the resource consumption and thus the cost of running a node significantly. And so in more detail, what this means for attesters, for stakers. As I mentioned earlier, the CPU requirements to verify these proofs is extremely minimal. As we sort of get rid of this notion of holding the states, state caching is not necessary to the extent we do it today anymore. So REM requirements are minimized. And obviously one of the biggest issues is the ever-growing execution layer state. So it's two terabytes today, maybe four terabytes. And as there's no need to hold the state anymore, we can get rid of, like, mostly rid of this requirement. And also, and Carlos also alluded to this a little bit, there's this significant advantage to, like, instead of syncing a node as we do today within days, we don't have to download the whole state history anymore. So syncing a node will effectively happen within seconds as you download a certain set of proofs up to a recent checkpoint and sync up to the tip of the chain immediately. So in conclusion, for stakers, we lower the barrier to entry to run a verifying node massively while at the same time scale the network dramatically as well. Oops, sorry, yeah, that was this. So where are we at? We have an EIP-8025 optional execution proofs. There is a feature branch on the consensus specs, I believe, already. There's also spec changes to the execution layer which have been published as well. And basically what this EIP introduces is a shift in the node architecture itself. So on the left-hand side, you see the ordinary, like, full node completely bundled with all of its tasks and duties as of today. And on the right-hand side, you see this ZK attester client architecture. And basically what this is, is you have a stateful consensus layer client, so not a lot of things change there. But instead of this engine API interactions with a stateful EL, you have integrated execution verification functions. And think of it as this lightweight verification function to verify these ZK-EVM proofs. And you can do this statelessly. And notice the handshake symbol. Oh, so, well, I mean, why would we do this? Is this amazing? What we do is we compress two binaries into one, which is a great thing. And also we remove a ton of lines of code, right? Most of the engine API, if you will. And, yeah, but notice the handshake symbol. Like, it's not sort of a full node as we know today. So we are effectively leaning into this unbundling of node roles going forward. And I go into this a little bit now. So zooming out, like, we have this ZK attester node, partially stateful or stateless. And attester node verifies the tip of the chain. But someone still needs to hold the state, right? So someone still has to be able to build a full block and serve state, if you will. So the role of the builder will continue to exist in the future, which is okay because we have Fossil. So all of the guarantees we want from a full node, like a local block builder today, will be preserved by the introduction of inclusion lists going forward. So this is fine, if you will. But what we introduce with the ZK-EVM proofs is this prover role. And now you may ask if attesting becomes super lightweight, who does the heavy lifting, right? So we have this asymmetry in node roles, if you will. And the proving task is a heavy duty. It's a resource intensive task. So currently, you need eight to 16 GPUs, like at current gas limits, to craft a mainnet block. And also, like an additional constraint is that this needs to happen within a slot, right? Because we need sufficiently enough attestations on these proofs in order to propose the next block. So we have this notion of, quote-unquote, real-time proving. So we need these proofs within 12 seconds to happen. So you may think, okay, isn't this asymmetry a bad thing? Like, doesn't it introduce a centralization vector, right? But the good news here is sort of we only need one entity within Ethereum, like one stakeholder, one prover on the planet to craft these blocks in order to stay live. And so this is a very generous and fortunate assumption to make by contrast, for example, to the honest majority we need in order to keep consensus alive. Right. So for stakers, this is a bit of a deep dive. Like, most of this will be abstracted away from you, but I wanted to show you, I guess, a little bit how the backend works, right? The whole flow from crafting a block until it sort of is gossiped and received by the ZK attester client itself. And so let's go backwards. So on the lower left-hand side, you see the ZK attester client, which verifies ZK proofs, as I alluded to earlier. And then in the gray rectangles, you have this prover maybe bundled with a builder, maybe not. Could be two separate entities. So on the lower right-hand side, you have this prover who runs this ZK virtual machine. And think of this as this proving backend, like where the cryptographic magic happens.


in order to prove correctness of execution. And there are certain inputs which the prover needs to feed into the ZK virtual machine. And this is A, a guest program, and basically this is a technical term for an execution layer client. So it's basically the Ethereum state transition function, which can be like a GETH, SDF, Rest SDF, etc. And this guest program needs another input, which is A, the block, and so-called execution witness. And the execution witness is produced, like both are produced by the stateful execution layer client, which would be a builder, for example. So A, the block, and B, this witness. And the witness is basically just a data package to validate the block statelessly. So these are all the inputs, and the prover takes all these inputs, generates these proofs, and sends it to the attester. Now, some of you may ask, okay, what does it mean for client diversity? And basically, we want to preserve client diversity, like the notion of client diversity in protocol going forward. So instead, so and how do we do this? So basically, the idea is to introduce this multi-proof logic. So some of you may have heard of vouch or vero in the context of client-side cross-validation today. And basically what ZK attesters will do in the future is they will do this automatically. And the reason is that we can basically require them to download, let's say, five proofs. And as soon as three of the five proofs are deemed as valid, like they've verified these ZKVM proofs locally, they attest to the block. So, yeah, I mean, there's a, and this is important because provers can prove bugs, provers can, proofs can arrive sort of late. There's all sorts of, like we rely on, still on nascent cryptographic solutions. So, yeah, client, proof diversity is sort of our hedging mechanism against this. Right, so how are we gonna roll this out? And yeah, as I mentioned, it's gonna be a phased rollout. Like there are going to be multiple EIPs along the way. We'll start with optional proofs, as I mentioned earlier, 8025. We're currently targeting this for the H star fork. And basically what is expected to happen is that only a few validators will opt in from the beginning. And the basic reason is that proving in this regime is effectively altruistic, right? So there is, it's not incentive aligned to prove these blocks just yet. So, like for attesters, you may, like if a block doesn't, if a proof doesn't arrive in time, you will miss out on timeliness rewards, for example. So, and also, like very likely, like we wanna, as it's such a massive shift in the architecture of the protocol, we want to do a safe rollout, right? So probably in the beginning, stakeholders will continue to run this execution layer state in parallel, right? As a fallback mechanism, right? So this is valuable for us to sort of have this hybrid attesting world, if you will. And sort of there's still this execution, like stateful execution layer to fall back to, but we can start gathering, yeah, valuable mainnet data in this environment. And then in the next phase, another EIP, probably targeted for iStar, we will want to introduce this notion of mandatory proofs, or what I call here native proofs. And the reason is that native basically means all attesters are required to prove these ZKVM proofs and it's, and this is, this is possible because we suddenly have this incentive compatibility between building a block and proving a block, right? So in this regime, a builder has the strong incentive that all of the attesters indeed verify a proof of the corresponding block in order to land the block itself, right? So yeah, this is sort of the mandatory or native proof regime. Imagine us being at maximally higher gas limits by then. You could still sort of re-execute these bigger blocks if you wanted to. So it's possible on like higher hardware requirements to run a stateful EL, but it's not required anymore. Okay, talking about native proofs or mandatory proofs, there's this neat concept, which is very interesting in the context of scaling Ethereum called native rollups. And basically once, and sort of the connection here or the complementary notion is that once all of the L1 validators sort of are aware of the ZKVM proofs, what we can do at the L1 is we can expose the EVM itself to rollups with, for example, a pre-compile functionality. And why would we want to do this? So today these ZK rollups, they try to be EVM equivalent, at least many of them. And this is an engineering heavy task because whenever the EVM changes, they have to change their proving, their circuits around the EVM or their EVM implementation themselves. So what if we could just have them call a pre-compile and basically whenever the L1 governance changes the EVM, rollups automatically inherit this functionality. And so basically they get the automatic upgrades and sort of can get rid of all of their security counsels to protect for changes to their EVM themselves. And so as a goodie, rollups, like all rollup transactions would be verified by the L1 validator. So it's this, we make rollups much, much more secure because we have this infrastructure for proof verification on the L1. And yeah, so economic benefits, security benefits for rollup. This is a super exciting topic. Make sure to check out Luca's from L2Beat work on native rollups and do reach out if you wanna collaborate on this. Okay, final slide, I believe. So what are our next steps? Like what's the plan here for mostly the ZKVM team at the Ethereum Foundation? So basically for 2026, we A, have to make sure that we standardize a lot of the inputs for the proof generation process. So the execution witness, as I alluded to earlier, we need to have cross-compatibility amongst the guest programs, right? All the different state transition functions. We need to have standardized interfaces between the proving backends, like this motor for ZKVMs and the execution layer. We have to build the clients. And the good news is like we have prototypes for AD25 in Lighthouse already and Prism already. So we're hoping to get to a DevNet, a multi-client DevNet fairly soon. We also have to build the prover infrastructure. This is particularly a software development, I guess, question, less than a hardware thing. And yeah, at the end of the day, we need to benchmark these. They have to be performant. So yeah, data collection is one of the, another major subprojects. And yeah, at the end of the day, security, security, security. We rely on modern cryptography. It's, yeah, before we ship this on mainnet, we need to have the highest, highest possible security guarantees we can. And yeah, that's basically it. Make sure to join the technical, mostly technical discussions on the Ethereum Discord in these two channels. Make also sure to take a look at the ZKVM team's website. We produce a ton of blog posts regularly. And yeah, I think we have a couple of minutes for questions. And yeah, that's basically it from my side. Thank you very much. Hello, so if we can do remote attestation of computation and storage, at what point do we not need a blockchain at all anymore? I guess there is a use case for strict global consensus. So I think, yeah, like from my perspective, there will always be use cases for like global consensus. So essentially for blockchains. Amazing, thank you. Thank you.

