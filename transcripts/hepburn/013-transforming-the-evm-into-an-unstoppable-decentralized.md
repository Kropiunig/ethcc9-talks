---
title: "Transforming the EVM into an Unstoppable Decentralized"
speaker: "Anthurine Introducing web3://"
organization: "EthStorage"
stage: "Hepburn"
youtube: "https://www.youtube.com/watch?v=CUQkX9TToPU"
---

# Transforming the EVM into an Unstoppable Decentralized
**Anthurine Introducing web3://** (EthStorage) --- Hepburn Stage

Hello? So, I can start? Okay, hello everybody. So it's my great pleasure to be here to present Web2URL, which turns the Ether into what we call Unstoppable HTTP server. So a quick intro of myself. My name is Qi. I'm a founder of Isostorage and Quachain. I've been working in this space for more than eight years. Before that, I was a software engineer at Facebook Google working on their infrastructure system. And so that's why my primary interest in Web3 is in Ethereum scaling in terms of example, Layer 2 and peer-to-peer. And so we also received a couple of grant supports from Ethereum Foundation, also includes a grant from personal grant from Vitalik. So, first of all, what is Web3URL? It's an access protocol, which is similar to HTTP, but rather than using centralized, for example, domain names and also server, we're using Ethereum as the corresponding server and use smart contract to host the corresponding server cycle to tell what kind of the web front-end you want to display to the users. So here is an example of the Web3URL format. It's very similar to HTTP, but we replace the corresponding domain with the smart contract, and also it supports multi-chain so that we can put the chain ID, and also a way to how to call the smart contract so that we can know how the smart contract would like to return the web content by executing the smart contract on-chain. It's defined in the EIP for 804, which was finalized I think two or three years ago. So the question, why we want to have this protocol, what is the starting point? So when we developed this standard about two or three years ago, we saw this great trend of fully on-chain applications where not only the protocol is running on top of smart contract on top of Ethereum, but also a lot of metadata, like front-end images, are also on-chain. So one particular example is NFT, where we saw a couple of the NFT projects now they are moving all of their metadata, including images, on-chain so that you buy not only just a simple ERC token ID, but also corresponding all the assets that can be found on-chain so that people can always access this content rather than four or five years ago, some of the NFT images just disappear because it uses centralized servers to host all these images. However, when we take a closer look of all this NFT data, it uses third-party, for example, Moonbirds, which is famous for using this images pixels on-chain. If we check this token information, then it's still using a centralized server, HTTP, blah, blah, blah, something to display the corresponding images. Another famous is called Cyberbrokers. They upload a lot of vectorized images on-chain using SVG format. But if we take a closer look on this image data, it's still using IPFS. So to our surprise, those data are already on-chain, on top of Ethereum. They pay half millions or millions of dollars of Ethereum to upload this data, but there's no way on top of Ethereum to access this data directly, so we still rely on third-party services. And so, hey, we have to use something new to change this. And that's our starting point to develop this Web3 access protocol. So the motivation is that with this Web3 access protocol, with a very HTTP-like link, now you can call a smart contract and tell, hey, what is the corresponding data that you want to display on website? So that we can put in this, in the future, for example, we can put this Web3 URL link, and then we can directly grab corresponding data, corresponding images from the blockchain network. And so OpenSea actually already support Web3 standard besides IPFS or these centralized servers earlier this year, so that now when you browse OpenSea, actually you will directly find the corresponding images on-chain and display on the front-end. And so to do so, then the corresponding metadata of these images now can be just replaced by a Web3 URL link telling which contract you want to call and what is corresponding info of these images, and then it will return those images for all these NFTs. And all those things are actually live on top of the Ethereum network. And so how to use Web3 URL? Actually, it's very similar to HTTP. It inherits most of the HTTP features, except that we replace the centralized server entities, all these things, by a decentralized authority. It can be a smart contract, address hosted by the full address, or an ESNA name that register corresponding address of the contract. And second thing is a way to how to call the contract so that the contract will return corresponding front-end data. And lastly, additional HTTP information that tells what kind of additional format of this data, for example, MIME, and for example, error code, a couple of other things that you can define in this Web3 standard. And to use Web3 URL, here is from a user perspective how the Web3 URL will interact. So a user will type the Web3 URL link in the browser that supports Web3 URL. And then the browser will pass the corresponding information through JSON RPC of Ethereum and tells what is called the corresponding address, what is the call data, and how to call the contract, return the content. We know that HTTP is a composable protocol, so HTTP page can contain a couple of the subpages. So this will further turn these subpage calls into future further Web3 calls for, for example, images, JSON file. And then so this process repeated until all the content has been resolved and then returned to the front end of the user. So for the relative address resolving, this is basically directly inheriting from HTTP so that we can fully power the existing feature of HTTP while just replace corresponding centralized component with Ethereum. And so how to access this on-chain data with Web3 URL? So right now we are still working in a similar way pathway as IPFS. Now we have a gateway, which is open source, so you can run also your gateway. And then the gateway will just replace corresponding contract with subdomain and call also the corresponding call in the link. And then now you are able to access the corresponding content which will call the corresponding Ethereum JSON RPCs, the calls for the front end. And we also support basically a Firefox plugin which will allow you to just type this Web3 URL link and then we will automatically translate to the gateway address so that the user can also just directly access without knowing how to translate the Web3 URL to HTTP. And we also support a community project which he built a standalone EVM browser so that it can directly on the front-end side to directly pass this Web3 URL to corresponding contract call and display play the content. So this is super powerful which means that we don't rely on any centralized component, just directly at a user side perspective to complete all the display and calls and display all this task on the client side. So this is kind of our goal of fully decentralized access. So for applications, this can enable a lot of applications. Imagine all kinds of applications built on top of HTTP as long as all data is public. For example, Wikipedia, all kinds of the public information now can be built to corresponding fully decentralized counterpart using our standard. So one application is NFT images, which I also gave an example. And we skip these details. And for example, we also create core personal website using Web3 URL, which using smart contract to simulate a file system so that now we can upload the data.


data like just like FTP, but this FTP actually is hosted by the smart contract running on top of Ethereum. So then now I can synchronize my local HTML data website. This is an example of Vitalik website and on top of the smart contract hosted by Ethereum network. And now we can access this website without worry about any centralized component that may be compromised. So this is another example that we have already actually running on top of Ethereum net. And there are more applications that are exploring. So for example, one interesting idea is to build a decentralized Git so that maybe one day with the scalability roadmap of Ethereum, both in terms of computation and storage, we are able to have the Ethereum to hold the full, for example, Go Ethereum GitHub pages. And so then now we can develop Ethereum on top of Ethereum without any centralized components. I think this is really cool. So those are right now the current status. And so we also have a couple of blockers during the development. One thing is definitely we want more native applications support, especially browsers. For example, even though we have plugin, but plugin still rely on some centralized gateway. So we want to work with, for example, popular browsers. Actually, a couple of engineers from Google, they are really interested in this idea. They also have a lot of conversation to see how they are able to support this standard. And we also have support from OpenSea already this year. And now it's already fully supported by OpenSea so that when you browse on-chain images that NFTs that we have on-chain images, then OpenSea will automatically call the smart contract and return the data and display the content for you. And we also found a couple of issues like there are some malicious reporters saying that, oh, try to block us and say, hey, there are malicious content or this thing. Actually, this similar thing happens to IPFS gateway. And so it just takes more time to convince them, hey, this is a legit thing. All the things are on-chain and we just like transparently relay all these data from Ethereum. So another issue is regarding the storage. So we know that Ethereum is moving towards its new scaling roadmap while increasing the blocks limits of the L1. On the same inside, there's also storage issue because Ethereum, even though they are able to process more transactions while a lot of fancy BALZK technologies, but still they need the network to store fully every node would store a full replica of the data. So there is also motivation to how we are able to scale this data, especially with the new Ethereum roadmap, which we will basically put a lot of data using the peer DAS, including block space. So we are also working on this direction. We call it Ethereum storage, which receive a grant from Ethereum Foundation. And the basic idea is to build a storage native system, a layer 2 storage system on top of Ethereum and use this smart contract to verify the corresponding off-chain storage network. And we also, actually the standard has been also involving there are a couple of sub standards that just like HTTP, we know HTTP have tons of the standards that involve HTTP 1, 1.2, blah, blah, blah. So we are also evolving very quickly. We have a couple of sub standards to support different types of HTTPs. So for example, we can also support like standard HTTP request header and send standard HTTP response, which is exactly the same. You write a HTTP handler in terms of your like using Node.js or using Golang. But here it happens in smart contracts. So we have specified standards so that you can have fully utilize existing HTTP power to do more advanced applications. So that's today, that's all the presentation. And I would like to answer any questions. Thank you very much. Questions? Thank you for the presentation. So I see the utility for what's read-only content that we can put on chain and we just fetch it from smart contracts, etc. But do you have a vision or idea about write content when we want to update it? Yeah, that's a really good question. So, you know, HTTP, they have a couple of ways to interact with HTTP. There's a GET request, there's a POST request, and also DELETE request. So right now the standard is only defined for GET request. So basically just return the latest verifiable data from Ethereum. For example, one example I would like to highlight is, for example, the Bybit frontend attack. The reason is that the Bybit frontend is, the server is compromised so that it returns the malicious content because there's no way to, or there's a browser does not support a way to check integrity. So suppose the website is fully put on chain, then now we are able to verify it and also be able to make sure it's always available as long as the Ethereum protocol itself. So that's the motivation. And for the POST one, actually because for the Ethereum, like we know we have to send a contract not to modify the content. So for that sense, we haven't do a lot of work on that side, just more work say that there's a couple of other standards to say how to use a link to compose a transaction and sign it, modify. So, but that's not the major direction that we are currently working on. Hello, thank you for the presentation. I see you will work with the different browsers. What about Brave? You will work with Brave also? So actually I should put the logo there. Yeah, yeah. Actually, we would like to work with them, but I think, you know, it's, it's, it would be easier to have some like early adoptions before we come into like those browser. For example, I can tell one of the major browser here, when we talk with them saying, Hey, we have this standard. We want to work with you to support it. Do you know what was their response? I don't want to name, put their name. They say, Oh, give me one million. I will do it. I thought, if I have this money, then probably I would put it to more adoption rather than just say, Hey, I just buy your engineer to support this feature. So that's the current issue we are facing. Thank you. My, the extension is why don't build a browser by default who use the protocol Web3, not HTTP. Actually, we, we have a community browser, this one. So it's a prototype to demonstrate. Now we can use the existing kernel of the browser and then with some UI interfaces, now we can do the same thing, but definitely you should, you have to configure the RPC, all this information, but it works. Excellent. Cool. Thanks for the, all the attending the talk. And if you have any questions, feel free to contact me offline and then I would love to see how we, I can make the Web3 better.

