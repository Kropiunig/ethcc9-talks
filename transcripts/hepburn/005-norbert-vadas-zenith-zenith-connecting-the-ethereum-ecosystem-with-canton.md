---
title: "Norbert Vadas (Zenith) Zenith: connecting the Ethereum ecosystem with Canton"
speaker: ""
organization: ""
stage: "Hepburn"
youtube: "https://www.youtube.com/watch?v=hSC3vLVP5A4"
---

# Norbert Vadas (Zenith) Zenith: connecting the Ethereum ecosystem with Canton

Hi everyone, this is Norbert from Zenith, and I'm going to be presenting today about how we connect Ethereum's ecosystem with Kenton. So, let's dive in. Before we go into the details of what Zenith is, I want to quickly talk about two things. So, on the one hand, we've got Ethereum with its rich DeFi ecosystem, battle-tested DeFi applications, and also one of the largest developer bases out there. On the other hand, we have Kenton, which has institutional adoption at scale, processing $350 billion of daily volume, over $6 trillion of on-chain assets have been processed on applications on Kenton. You see that, for example, JPMorgan is launching JPM Coin on Kenton network, DTCC is tokenizing US Treasuries, NASDAQ is connecting their Calypso platform to Kenton network, and banks like BNY and Lloyds, they are issuing tokenized deposits. So there are quite a few things going on, and the latest news, I think, has been Visa just a couple of days ago coming online as a super validator on Kenton. So we do see that actually financial institutions do trust digital assets and its 10-year track record. There is a reason why they have chosen Kenton. On the other hand, of course, they also want higher yield opportunities for passive assets or previously passive or low-yield assets. They want mobilization for those assets in a multichain world, basically. And they also want global distribution and 24-7 markets for those assets. And actually, that's where Ethereum comes in, because to build an application on Kenton currently, natively, you need to learn DAML, and you need to basically rewrite your existing applications, and many of them already exist on Ethereum, in a new language, going through auditing, etc., etc., again. Now, this is a lot of risk. And that's where, actually, I think Zenith becomes super important and unlocks a whole lot of new opportunities. It's because we connect Ethereum's developer base to Kenton, and without rushing ahead too much, we basically allow you to deploy Solidity applications on Zenith EVM that then become atomically composable on Kenton and act just like as if they were deployed natively on Kenton. But let's take a quick look at that. So what is Zenith? We are basically extending Kenton's execution capabilities with EVM compatibility first, and then soon after comes SVM as well. As I mentioned, current focus is on EVM, and that means for Ethereum developers that they can natively deploy Ethereum applications on Zenith using familiar tooling, etc. We're going to be talking about that more. And these applications become atomically composable with Kenton, unlocking a whole new range of use cases that have not been really possible in the Ethereum and the L2 landscape because of the bottlenecks that the lack of atomic composability provides. And then for enterprises, it actually means that they can operate their own EVM environments, which are composable with any Kenton subnet. So atomic composability, we've mentioned that quite a few times, but more importantly, it unlocks multi-leg transactions. We are going to be talking a little bit more about this as well. And it unlocks true unified liquidity, which I think is going to be super interesting how that evolves. But let's take a quick look at how you can build on Zenith. So basically, Zenith EVM is the reference implementation run by our own team. And you can directly deploy EVM applications on Zenith EVM when we are live on our mainnet. We're going to be talking about our roadmap as well. This is a high performance network, 100k plus TPS. We don't promise millions. 100k plus as of now is pretty much enough for most use cases. And you can go as low as like you can have as low as 500 milliseconds of latency. You will have ready to use tooling, such as oracles, interop, indexing, and such. And basically with this, you can have fast go to market because you can lift and shift existing solidity applications. And then on the other hand, you have the Zenith stack, which allows teams, enterprises, institutions to build customizable EVM environments for Kenton. These are configurable on the economics level, on the fee model level. You can have your own native token. You can implement sequencing logic, etc. These are compliant and compliant environments where you can set up custom permissioning. And also, these have been designed with enterprises and institutional use cases in mind, particularly. Let's take a quick look at the technicalities on a high level. Zenith EVM is REST based, so it's a bytecode compatible EVM environment. We support all libraries. We support all standards, be it 3643 for RWAs or 4626 for tokenized vaults, etc. And we expose vanilla Ethereum RPC interface. So it's super easy to connect with. Regarding the atomic composability, there are two important things to mention here, I think. One of which is the external code primitive. That's something that we have implemented as, like the Zenith team have implemented in DAML directly. And this facilitates DAML contracts to deterministically call out to solidity contracts, send EVM payloads as inputs, trigger native EVM execution, receive transaction outputs, and also receive the EVM state route to settle that on Kenton. And with this, actually, I already started talking about the final two points on this slide, which are also pretty important. All EVM transactions are routed through Kenton. And I don't know how heavy that lands now, but this is significantly different than the current architecture of whenever people build an additional layer on top of something that already exists. So in case of an L2, we often get the question whether we are an L2. In case of an L2, basically transaction volume and gas fees and all the gas token utility, it moves from the base layer to the layer 2. Now, since we are routing all EVM transactions through Kenton and we have the external code primitive to allow DAML contracts to atomically interact with EVM contracts, basically you have an environment where we are literally an enshrined EVM extension, which where coordination is done on Kenton and settlement after the EVM transactions have been processed, state root settlement is also done on Kenton. I think we're going to touch upon this a little more, but I don't want to go too many details as of now. Now, how can you build on Zenith? As I mentioned, we are an Ethereum equivalent environment using Reth, etc. So this means your DevEx, if you want to deploy on Zenith, will be lifting and shifting your existing Solidity application, just replacing the RPC URL to Zenith's endpoint and basically using all the standard Ethereum tooling like Hardhat and MetaMask, you are good to go to deploy your application. It should be very similar and will be a very similar look and feel of how you would actually deploy on Ethereum. Now, with regards to interoperability, Zenith EVM, we are not a bridge as well, just mentioning it because we often get mixed up in that sense as well. So Zenith, even though we are in the middle from a certain perspective, we are connected to the broader Solana, Base, Ethereum ecosystem through interop solutions like LayerZero or CCIP and others. So that's one angle. But then on the other angle, we have atomic interop, which we are implementing ourselves between Kenton and Zenith EVM. And this is important because we don't want to break the atomic composability that's a given through biorouting all transactions through Kenton and having the external code primitive to allow multi-leg operations.


operations executed in a single transaction. So you have this interop and from this perspective, basically you can move any existing assets from current large ecosystems like Ethereum or Solana or base to Zenith EVM and start composing them atomically with any and every Kenton application and subnet. Now, how does that composability look like? So as I mentioned, all EVM transactions are routed through Kenton. This essentially means that basically we allow Kenton users to submit EVM transactions directly from Kenton. The EVM payload is wrapped into a native Kenton transaction that lands with Kenton's synchronizer. This native Kenton transaction is composed of a set of operations, like a tree of operations, which include a Kenton leg, let's say taking some asset native on Kenton, and we want to swap it to some asset on the EVM side. So let's assume we have two legs there. This set of operations includes all the Kenton side, the operation start, and then comes another one, which is the external call. It takes the wrapped EVM payload, shoots it to the EVM side. It triggers native execution. And then it receives all the transaction outputs and also the new state routes after the execution took place. So basically, the external call is step two, the invocation from the dumbbell contract to the EVM. Yeah, that's maybe something worth mentioning. Our EVM has a contract on Kenton, a dumbbell contract living on Kenton, which registers incoming transactions, communicates with the EVM, and also takes the EVM state routes and settles them. So that's basically coordinating the composability element here. So after native execution, all the transaction results are received by the dumbbell contract. There is a deterministic validation because all validators involved in the transaction are re-executing basically the transaction. And that's how they validate that either all post-transaction results are the same or the transaction fails. And then there is subsequent atomic settlement and finality, which is done on Kenton. This allows, like to put it in a more practical perspective, this allows something like you can bridge an asset from or move an asset from Kenton to Zenith EVM. You can borrow against that asset. You can swap what you borrowed to another asset to get into a certain position. And then you could even hedge against that position and settle all these legs in a single transaction atomically. So you can execute complex strategies across Kenton and Zenith EVM. Also another aspect could be that real-world assets could become instantly usable in DeFi because you can purchase some tokenized asset and within the same transaction as a second leg, you could put that up as a collateral in a DeFi protocol and start earning yield on top. And this basically eliminates the execution risk between legs and also enables institutional-grade transaction logic and transaction flows, which is pretty much unheard of in the EVM space as of now. And another aspect I think absolutely worth mentioning is unified liquidity. And I mean true unified liquidity. Due to the architecture of Zenith and the atomic composability that we unlock, you can have atomic swaps across any Zenith chain and any Kenton subnet. For example, you can have cross-domain liquidity. You can have complex DeFi operations spanning both networks and spanning applications living on those two networks. And you can have multi-chain settlement of these complex workflows in a single transaction where Kenton acts as the coordination layer and also the settlement layer, as I mentioned earlier. Now, we are coming up on time, so a quick overview on where we are. We have a testnet implementation, end-to-end transaction processing across a Kenton testnet network and Zenith EVM deployed on top of that. This has been completed early March. We are looking to have mainnet readiness on Kenton by the end of Q2. And the production go-live of Zenith EVM mainnet on Kenton mainnet is expected to come in Q3. And even though our testnet is actually an internal testnet as of now, we do have a public explorer which I quickly want to show you if I can bring this up here. Yeah, very good. So we processed almost 200k transactions here. I quickly want to show this element to you. So you see that we have approximately 195k transactions and you see that we've got the same number of external calls here. This means that every single transaction that hit Zenith EVM was routed through Kenton and had to utilize the external call primitive to trigger the EVM execution. And you also see that all transactions have a Kenton reference update and also an EVM hash. So you can see that you can actually have an overview on both sides of the transactions, basically. If you... Yeah, let's take a quick look at the network and maybe this is something good to close with. So we shoot a transaction to our network every couple of seconds. We have an EVM node here. Oh, by the way, this transaction that you see on the screen, it's about someone directly shooting a transaction to Zenith EVM, which then gets routed to Kenton, also can process a leg on Kenton, and then settles back to Kenton. So the way it happens is the transaction lands in the EVM node. It gets prepared. The relayer wraps it into a native Kenton transaction. It's submitted to Kenton's synchronizer. And then there is validator 1 running actually the EVM node, the relayer, and the external call server. So validator 1 processes the transaction executing both legs locally and posting the signatures and the transaction results back to the synchronizer. Validator 2 re-executes everything, including the external call and the native EVM execution locally, also sends back the results to the synchronizer. And there is an entity called observer, which is not common or not usual in the Ethereum space. They validate transactions taking without running the external call. So taking the external call results from these two participants, they also shoot back the validation results and their signatures to the synchronizer. Once the synchronizer has checked that all signatures match and everybody agrees on the post state, then everybody, the transaction reaches finality and everybody updates their own local states on their own nodes. So that's where we are now. In case there are any questions, I'm happy to discuss further. Other than that, thank you very much for listening to us. Please take a look at our website. We've got some docs out. Also happy to chat on our socials. And yep, thanks a lot for being here. Thank you very much.

