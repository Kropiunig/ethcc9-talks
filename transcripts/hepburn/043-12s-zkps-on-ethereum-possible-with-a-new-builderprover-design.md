---
title: "12s ZKPs on Ethereum Possible With a New Builder–Prover Design"
speaker: "Leo Fan Making Sub"
organization: "Cysic"
stage: "Hepburn"
youtube: "https://www.youtube.com/watch?v=VBaWMmAB000"
---

# 12s ZKPs on Ethereum Possible With a New Builder–Prover Design
**Leo Fan Making Sub** (Cysic) --- Hepburn Stage

Hello, hi, my name is NeoPham from Synthetics. Sorry for the delay. Yeah, so in this talk I'll show like from computation graphs to like production level ZKVM nodes. Like we did like basically rewrite a lot of things like in the ZKVM tech stack. So the talk will be like three parts. The first is like we compare the difference between this like HAL, which stands for hardware abstraction layer, approach versus our graph-based approach. Then the second is like we, like our ZKVM, we named as like Venus, and right now is the version number is 0.1.6. We do some implementation of it on the different hardware, such as like GPUs and also FPGAs. And then the third part is like how this like ZKVM node should work, maybe it should be like one, two, three instead of one, one, one. Okay, yeah, so let's first talk about like what is like HAL approach and then graph approach. So HAL approach that we wrap around like each kernels like using like these like APIs, and whenever you want to call this kernel, you just call the API and it will do the job for you. So it's like which this like approach supports running the same proving structure like in different hardware platforms. And the graph one is like sort of different. It encodes the whole process, whole proving process as a graph, and then you do some like translation, you do some like coding regarding to this like graph. Yeah, and we can see like right now, like the AI community, you only adopts the second part since it's like more fits to the like the GPU like architecture. But the ZK community, like since, since like we start from Hello2 or Gnark, which is like pure like CPU execution, and we just say like if you recall there's some like Z price competition before where like people optimize the MSM, which stands for multi-multi scalar multiplication, and then like NTT, like we optimize like using GPU or FPGA to optimize these like kernels since like in the whole process, these kernels take a lot of time. But yeah, so that's like why like the normal like ZK community like they start from the HAL since they benchmark the process and thought like some part is like more time-consuming, so they want to optimize that part using some specialized hardware such as GPU and FPGA. Yeah, so here is like a normal or like previous or like several ZKVM like the how, how the coding paradigm is like. So you first, you like, you do some wrapper round on the, on the internal core option such as like reallocate your buffer or some other things. And then you build your like some, some kernels such as like a commitment kernel or like the NTT kernels based on this like internal code. Then there will be like the third step will be like a pull function, which like a glue over all these like kernels together. So that's like a normal like typical ZKVM, ZKVM implementation paradigm is like. Yeah, if we want to use a picture to illustrate the process, it's like we have the, say we have the like the CPU which will first run this like build command, then it will like call the GPU to do a lot of things, like such as like doing the commitment to evaluate the polynomial, evaluate the constraints. There will be some like communication between the like the CPUs and the GPUs since like probably you'll use like the CPU to do the Fiat-Shamir hashing and all the other things. So it's like this like streamline, like each hardware will do their like corresponding job and then they talk to each other. So that's the HAL mode. And of course, like we can see some disadvantages in this process are it's like since there are like many kernels which will happen on the, on the CPU, on the GPU side, so it's very hard to optimize the kernels from this global perspective. And these kernels, they need to synchronize with each other, which also takes up some time. And like more things like if you want to like start the kernel, start some chip on GPU, it also takes some time. So which means like it's difficult to achieve some global optimization. And second, yeah, since the second is like we treat, since the HAL will treat the things like as a black box and there are some interactions such as like the Fiat-Shamir transformation and the IOP reduction, which remain like a hidden from the proving process. And the third one is like it's hard to translate or like rewrite the codes like to some other platforms, since like if you want to rewrite it to like FPGA, then it takes really some time to do so. So that's some like disadvantages in the HAL approach. And yeah, then let's move on to say like how the graph mode is like. So to, to like, to warm it up, we, you only implement the implements the, the ZKVM, we have like three approaches. Like first is like direct implementation, which like you just directly implement the ZK proving function in Rust, in Python or in some other language such as like the Glock or like the Hello2, the original Hello2. And the second, like there is some embedded like DSL into this like direct implementation. Yeah, so that's the second layer. So it's like from shallow to deep. And the third one is like you design some like DSL such as like Carol or Noah like to encode the like how the function should work, how and so, but you need to compile it like into some other things to, to make it work. Yeah, so the HAL approach basically encodes the computation to be proved by the graph mode will encode the whole proving process, which is like more comprehensive. Yeah, so here is like the, we use a picture to illustrate the graph mode, which like the CPU will first compile this like whole proving process into a graph with into a like a deck. It's a pure data structure. And then like we run all these things on the, on the GPUs, like will be like continuous like GPU execution, no CPU like round trips. So like the GPU doesn't need to talk to the CPU and other things, which means like we can achieve this like a globalized optimization across like all kernels to make it like faster. And the kernels, they don't need to like synchronize with each other like too, too frequently. So like in our design, we have like three parts. The first is the circuit representation, which is like mainly used by the end user. We'll try to make it easier for the end user to, to use it. And the second is the IOP reduction. So IOP reduction care about, care about the correctness or the soundness of the, of your ZKVM. And then there is a high performance computation, which like, we just want to achieve like as fast as possible for, for this kernel. Yeah, so previously I was saying like the PO or CIRCOM, PO or PO2, like, then they separate the circuit representation from the IOP reduction and like high performance computation. But in our approach, like we separate like all three things, like, so like each part will just care about their like jobs and like don't need to like interact too much with other. Yeah, so here is the result. Like we, so our thing is built on Zisk. The latest version of this guy is, I remember it's like 0.16. We implement, so the comparison right now is like done on like 15.90 GPU cards and we achieve, so I, I mean like last week is like 6%, but right now we are improving. It's like almost like 10% for now. And the code is also on ZKVM, on its proof. Yeah, but like we are switching from, I guess, like from REST to guests. So like we are still catching up with the block. So the screenshot I take is like a, a day ago and we are catching up with the latest block. After that, we can do the proving. Yeah, so here is the result compared with Zisk. And as I just mentioned, like the, the encoding of the whole ZK, the encoding of the whole proving process will allow us like to translate the, the pure data structure into other, into other, I mean, into other hardware platforms as well. So we do, we also do it on, on FPGAs. The FPGA, like right now is a very preliminary result with just like HAL like friendliness synthesized the C++ codes. Yeah, we didn't do any optimization on it yet, and it's showing almost the same performance per watt.


That's as GPU. So for the metrics for the hardware, there are like two metrics. The first is performance per dollar, which means how much you pay for your hardware. The second is performance per watt, which means what's the energy efficiency of the thing. And I believe by optimizing the FPGA or not, then we can get some probably like slightly better performance in terms of like performance per watt compared against the GPU. So right now the FPGA just runs around 100 megahertz, and normally it runs around 300 megahertz. So still something like we can do. And yeah, since like Scythic, we started a hardware company, like we do a lot of hardware acceleration. And one of the goal for Scythic is to design some specialized ASIC chips for the A-K, but yeah, we are just not very sure if the market is big enough to digest the chip, but we still believe that if we ignore the upfront cost, the ASIC will still be the perfect, be the ideal hardware to run it. Since on ASIC, like the, we can basically decompose the module like into like many smaller ones, and this like internal module communication will be treated as like a first-class on-chip flow, which will make things like with much efficiency than doing on FPGA or like GPU. Yeah, so there are like so many like things like we can customize if the market is big enough, and we think like big enough to pay the upfront cost for tape out for chip tape out. Yeah, so here is our summary on the hardware. Like I believe like three years ago, we write a blog post to say what's our take on GPU versus FPGA versus ASICs. We still think it's almost the same as before, but right now the VK algorithms, they converge or not. So when we wrote the blog like three years ago, like there are so many VK algorithms in the space, but right now they converge or not. Like the GPU still remains the production performance pass. Like if you like it's still the main power, main proving power for the AK workload. And the FPGA, FPGA is still, we use it as a prototype for the ASIC. It represents some like architecture design, but it's not the end point. And one more non-technical thing about the FPGA is like, it's not very easy to acquire the FPGA chips. So which means like, yeah, you also need to pay a lot like compare against the GPU. And we use FPGA learnings like to quantitative as a quantitative guidance to the ASIC design. If there's still like needs demand in the market to come up with ASIC, then like we are very happy to do so. Yeah, so this is like how the VK EVM node should look like, and it's supposed to be a demo, but I don't have time to show it up. So it's, so like, I guess like the audience right now is like very familiar with VK EVM nodes, which like instead of re-execute the blocks, you just need to have some like provers to generate proofs for the block, then you just verify the block instead of like re-executing the block. Yeah, but there are like still some questions like we need to think about. The first one is like, what's the provers, what fits in the general like Ethereum roles? Will it be like some separate role or like it will be probably like the relayer should be the one to do the proof since the relayer can see the blocks and it will just choose probably like the most, the blocks has the most gas to do the proving. And then we need to design some like incentivizing rules to mobilize, to motivate the provers to generate the proof. If it's just the provers and to do the proof for each block and what they are going to get from the block since they are like without a proof, the gas per block is limited, but with the prover, the gas limit can be like hugely increased. And so what's the cost the provers should take like from boosting the gas limit? Yeah, so that's something like I guess the Ethereum foundation should think about. Yeah, so if you are interested in, like here is our code repository. We open source our codes several days ago. And we also write like three posts, like explaining the details of the graph modes and like our take on different hardware plus like what will be the VK EVM economics will be like in the in the post. Yeah. Okay, thank you. Thank you, Leo. We don't really have time for questions, but maybe people can join you in the. Thank you guys.

