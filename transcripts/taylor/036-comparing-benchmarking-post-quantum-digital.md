---
title: "Comparing & Benchmarking Post Quantum Digital"
speaker: "Alexander (Miden), Andrey Ivasko"
organization: "researcher"
stage: "Taylor"
youtube: "https://www.youtube.com/watch?v=ZA-Qoi7XkdQ"
---

# Comparing & Benchmarking Post Quantum Digital
**Alexander (Miden), Andrey Ivasko** (researcher) --- Taylor Stage

All right, hello everyone. My name is Alexander Lee. I'm a protocol engineer at Myden, and this is Andrea Vasco, independent researcher and developer. And today we're going to be talking about comparing and benchmarking post-quantum digital signature schemes. So before we actually talk about the comparisons and everything and our benchmarking results, we need to talk about why does this even matter? I mean, you guys have probably been following crypto Twitter and academic papers, and the likelihood that a quantum computer that is able to break elliptic curve cryptography is actually, like the time till that day actually comes is actually decreasing. So right now, it's not possible for a quantum computer to actually break elliptic curve cryptography, but within the next five years, there's a high likelihood that, maybe not a high likelihood, but at least a low-term risk. So within the next five years, if you are using elliptic curve cryptography in your protocol, you really need to be already underway in migration. 10 years out, NIST pretty much recommends completing migration to post-quantum alternatives by the year 2035. And Google actually recently came out with a paper that said that, I mean, in the past couple of days, it said you really need to be starting today. So yeah, and in 20 years out, it's highly likely that there's going to be a quantum computer that's able to forge signatures based on elliptic curves. And before we actually get into the benchmarking metrics and comparing these PQDSAs, and I'll talk about the families of PQDSAs, it's important to note that all these signature sizes are significantly larger than their elliptic curve alternatives. But we'll talk about the numbers later. So this is really going to affect the entire stack because signature sizes are significantly larger. Okay, so let's talk about post-quantum digital signature scheme families. So there's really five main families of post-quantum digital signature schemes. There's lattices. This is by far probably the best drop-in replacement for ECDSA. The downside is that the implementation is super complex. The math is complex also. There's also hash-based signatures. This is probably the most conservative from a cryptographic assumption perspective. The downside is they have much bulkier signatures. There's also code-based. So DSA is based on error-correcting codes. Also relatively conservative, but they have the downside of having large keys and signatures. Isogeny is a really interesting category. When we were actually creating this benchmarking repository, signatures sizes are really small. It's like 150 bytes. And ECDSA is like 64 bytes. BLS is like 86, I think, bytes. But the downside with isogenies is that the implementation is complex. Signing times are also really slow. Verification time is also slow. So there's some downsides there as well. Multivariate, fast signing, small signatures, but they have large keys. So as you can see, there's a bunch of different categories, but they have advantages and they have disadvantages as well. Let's talk a little bit more about hash-based signature schemes. So this is really, I would say, like old-school conservative cryptography. The ideas go back to the late 1970s with papers from Lamport and Merkel. The idea here is that you generate many one-time signing keys, and you place these one-time signing keys as the leaves in a Merkle tree. And so you have a one-time signing key. The public key of this one-time signing key, the hash, is actually the leaf here in this Merkle tree. So each time you sign, you actually reveal a leaf and the authentication path to this leaf, and the verifier of the signature actually just checks that this hashes to the root. The downside with hash-based signature schemes is that most of the implementations are actually stateful, meaning that you actually have to keep track of which leaf you used. In the case of a validator, like an Ethereum validator, this is actually okay because you only have one instance per machine of this Merkle tree. But if you had this XMSS type structure in a MetaMask wallet on different devices, you'd actually have to synchronize across these devices which leaves you already used. Otherwise, if you use the same leaf twice, you actually reveal information about your secret key. So yeah, the benefit here is that they have small keys. Again, this is just like the root of this Merkle tree, but they have relatively big signatures. So XMSS, this is sort of how it looks like. Again, you have this single tree structure. You really can only have like a max of like 2 to the 20 signatures, essentially like 2 to the 20 leaves, depth 20 of this Merkle tree. That's kind of a downside because, I mean, that's like as many times as you can sign. So there's this other implementation called XMSS MT, which uses like this hyper tree structure where you have smaller Merkle trees, and you actually can have more signatures here. So main examples are XMSS, XMSS MT, LMS, LeanSIG. LeanSIG is really interesting. It's like this lean Ethereum foundation, like Ethereum foundation's implementation of XMSS. It's this ZK-optimized re-parameterized version of XMSS. And also Sphinx+. So Sphinx+ is an example of a non-stateful hash-based signature scheme. And Ethereum Foundation's also, last weekend, we went to this like Ethereum Foundation workshop, and they had this really cool example of like a solidity verifier for Sphinx. So now let's talk about lattice-based signature schemes. So this is much newer compared to hash-based signature schemes. Security comes from hard lattice problems in the context of a lattice-based PQDSA. A lattice is a high-dimensional grid of points generated by basis vectors. The cool thing about this category of signature schemes is that they have like medium-sized signatures and relatively good performance. And like I already said, like this is much closer to like a drop-in replacement to ECDSA. Good thing here is that they're stateless as compared to hash-based signature schemes. Much better for user wallets, but really complex implementations. So when you're actually implementing this, you got to be super careful with implementation, like on the verifier. So yeah, like examples here are Dilithium. This has been standardized by NIST. Falcon512. At Myden, we actually use Falcon512 for authenticating state changes of accounts. So isogeny-based signature schemes. This is kind of, you can consider like an exotic category of post-quantum cryptography. This is built on isogenies, which are like these structure-preserving maps between elliptic curves. The super cool thing that we noticed when we were actually creating this benchmarking repository is that they have extremely like small keys compared to other PQDSAs and small signatures. The main caution here, though, is like cryptographers kind of don't trust this PQDSA family as much after SIKE was proven to be broken, which is like this supersingular isogeny-based key exchange. It's like this post-quantum version of Diffie-Hellman, and it was proven to be broken. So the trade-off here is the compactness of the signature size comes with heavy math and complex implementations. And again, like signing times and verification times are really slow compared to other forms of post-quantum DSAs. So the main example is like SQI Sign. I'd recommend after this talk, like take a look at this. It's kind of a cool signature scheme. So of course, there are other PQDSA families. There's CodeBase, which are built on hard problems from error-correcting codes. Examples are Cross and Less. When we were actually building the benchmarking repository, we didn't really see there's any benefits of these. Like they didn't outperform any other PQDSAs. There's also multivariate, quadratic system-based. These are built from solving systems of quadratic equations over finite fields. Mayo, it's cool because it has relatively small signature size, smaller than Falcon. And there's also Stark-based, but there aren't any papers or implementations that have been submitted to NIST. But this is also interesting. So these are all promising alternatives, but they really don't win over lattices or hash-based PQDSAs when it comes to just like simplicity and conservative assumptions in the cryptography. So now Andrei is actually going to talk about this benchmarking repository and talk about the performance and the metrics. So yeah, I'm going to hand it over to Andrei. Yeah, thanks, Alex. I will jump straight to the numbers. So we used the following benchmarking setup, host machine, MacBook Pro M1, 16 gigabytes of RAM, and the message size is about 32 bytes. We applied the following benchmarking methodology. So we benchmarked following metrics, signature size, key size, signing time, and verification time. So the signature size impacts bandwidth and propagation speed. Key size impacts storage overhead for wallets validators. And signing time and verification time impacts user latency and validator throughput. Regarding signature size, it affects propagation, bandwidth, and storage. So we benchmarked these post-quantum signature schemes, and we identified top winners here. The SQI Sign, Mayo, and Falcon. SQI Sign is isogeny-based, Mayo is multivariate, and Falcon is lattice-based. In terms of verification speed, it affects validator throughput. And top performers in this category are Falcon


And Delithium, they are both lattice-based, and HSS is a hash-based. And the signing speed affects latency and feasibility on hardware wallets. And we identified three winners in this category. So the signatures with the fastest signing speed are Falcon, Delithium. They are both lattice-based. And Cross, it's a code-based. Also, we measured the public key size. So it affects storage overhead. And we identified top signatures with the smallest public key. They are Sphinx Plus, LinSig, and LMS. They are both hash-based. And next, I will cover engineering application trade-offs and implementation risks. In terms of engineering application, I would like to point out that lattice-based signatures, they have a best overall balance, and some of the implementations, they are NIST standardized, for example, like Delithium, but the implementations are quite complex. Regarding hash-based signatures, they are simpler primitives that are lattice-based, although they have a large signature size, and some of the implementations are stateful. Code-based signature schemes, they are relying on the hard decoding problems on error-correcting codes, but the keys are very large, and there are some security concerns by NIST, so NIST has not standardized any code-based signature scheme yet. NIST just takes more time to take a decision and evaluate its design and security. Regarding isogeny-based scheme, I would like to point out that they produce extremely compact signatures, but the downsides here, that's the signature time is slow, and they have also, NIST has raised some security concerns regarding this family of the signatures. And regarding multivariate signature schemes, I would like to point out that they are relying on a heavy mass, although they produce fast signing and small signatures, there are some security issues. And recently, it was discovered rainbow hack. Rainbow is a multivariate NIST candidate post-quantum signature scheme, and it was susceptible to algebraic attack. Algebraic attack, it's an attack in cryptoanalysis where the attacker exploits algebraic structure of a signature to retrieve some sensitive data from the signature. So the performance versus confidence in security is a real trade-off. I visited the EF workshop over this weekend, and Federico Caron, the founder of LambdaClass, mentioned that complexity is the enemy of security, and I fully agree with him. So more mathematical structures can buy smaller signature and faster signing verification time. However, more complexity can lead to larger attack surface and implementation risks. So on the right side here, on the chart, you can see that multivariate and isogeny-based signature can offer better size-speed efficiency, although they have less confidence in mathematical assumption than hash-based and lattice-based signatures. So it's worth noting that implementation risks can arrive before quantum risks. I would like to point out, if you implement post-quantum signature, you have to take into account all implementation details, parameters, and intricacies. So, for example, like Delithium, it's a lattice-based hash-based signature, post-quantum signature. Delithium has many sensitive intermediate values and rejection sampling logic to harden. So Falcon relies on a floating-point arithmetic. And if you implement your signature and there are some security issues, it can be susceptible to side-channel attack. So side-channel attack is a type of attack where the attacker exploits some, for example, like electromagnetic waves to recover sensitive data from your signature. Post-quantum Ethereum. So Lean Ethereum is an Ethereum initiative to make Ethereum post-quantum. And so right now in the consensus layer, Ethereum uses BLS to aggregate signatures, but BLS signature is not quantum secure and they want to replace it by a hash-based signature, for example, like XMSS, but XMSS lacks a native aggregation property. That's why they want to apply SNARK-based aggregation to aggregate XMSS signatures. So let's take a look at the NIST standardization snapshot. So as we can see here that Delithium and Sphinx Plus, they are already standardized and LMS and XMSS have been standardized as well. And the next likely addition will be Falcon, but NIST is still evaluating 14 additional candidates in the on-ramp round. So the final takeaways are no single family wins every metric and Delithium can be the strongest candidate for broader deployment such as Ethereum execution layer of Web2. And the hash-based signature can be deployed where the conservative assumptions matter more than signature size. So thank you and you can take a look. This is our repo with benchmarking and you can play with our post-quantum signature schemes. Thank you. Right now we can take some questions. Thanks for the presentation, guys. Have there been any cases in the past where NIST or any other standardization body has rejected a scheme because it was complex to implement? I would say Falcon has taken longer than expected to standardize by NIST simply because Falcon uses floating-point arithmetic in the key generation and also signing process. So yeah, I wouldn't say there's been a rejection. I don't know of any rejections, but I know they've just taken longer. They're looking at the signature scheme, but they're not actually accepting it. But this is on the spec side, right? It's not an implementation yet. Yeah, exactly. Yeah, guys, thank you for the presentation. If I'm planning to implement something now, what would you recommend? What would be the best? It depends on your requirements. Do you need aggregation of the signatures or need... It depends on your requirements. Now, I would say, honestly, I'm a little bit biased. I like Falcon-512 because we use it at Myden. But yeah, if you're looking to aggregate signatures, I would do what Ethereum Foundation is doing with this aggregation of signatures using XMSS inside of LeanVM. I mean, it's so new. I would say, honestly, for a broad deployment right now, maybe Delithium, since it's standardized by NIST. Presentation, you list like coding-based, code-based signature scheme is like faster than hash-based and also lattice-based. But in my memory, it looks like code-based schemes are all pretty slow. So is there any new progress? Because I didn't pay attention to that part for quite a long time. Yeah, so it's interesting because there's Cross and Less, and they have completely different performance metrics. They're both code-based, but I think Cross has much slower signature verification. Honestly, I would need to look more into code-based. But we talked to a bunch of people at the workshop last weekend, and they're not very... Most cryptographer people that we talked to are not very confident in code-based signature schemes. So I don't really know if it's worth pursuing even. Yeah, at the Ethereum implementation, like cryptography team, they're not confident about code-based, isogeny-based, and multivariate-based. So they told us, like, we have to wait until NIST will standardize them. Any other questions? We have a last question here, and you can keep the conversation after that. Thanks for the great talk. I was also asking, like, the implementation risk, on which side is it, like, when I use it as a library or, like, when I build it on x86? Or, like, where does the implementation risk... Like, whose implementation risk? I would recommend you take a look at Slack or internal messages. There's something new there. But, yeah, like, implementation risk is definitely in the verifiers, like, the most critical piece. Like, the verifier is, like, super critical to make sure that the implementation is, like, correct. So, yeah. Okay, one last question. Thanks. So we know that Mitten is using Falcon. Do you know of any other blockchains that are using post-quantum signature schemes already today? Yeah, I think...


There's like Neptune, I think, maybe is using post-quantum signature schemes. There's a few. They're like really small, kind of like almost no-name projects, but they do, like, say, like, we're using PQDSAs. I think Neptune uses some kind of stack-based, but I'm not quite sure about, like, details, so. I guess this is due to limited block space. Yeah. So thank you for your presentation and thank you for your questions and answers. Thank you.

