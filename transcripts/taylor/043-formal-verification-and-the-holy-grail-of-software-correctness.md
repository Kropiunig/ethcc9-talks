---
title: "Formal Verification and the Holy Grail of Software Correctness"
speaker: "Charles Cooper"
organization: "Vyper"
stage: "Taylor"
youtube: "https://www.youtube.com/watch?v=4mRHvbNbxkM"
---

# Formal Verification and the Holy Grail of Software Correctness
**Charles Cooper** (Vyper) --- Taylor Stage

Okay, hello everybody. I'm Charles Cooper. I'm the lead developer of Viper. And today I want to talk about this very fun title, which is the holy grail of software correctness, formal verification, and how we make sure that everything is correct. So first of all, what is formal verification? Quick background for people who aren't familiar. Formal verification is basically the use of mathematically rigorous techniques, logical steps to prove that software is correct. So we want software to meet, this is kind of a philosophical question, right? Like what does it mean for software to be correct? And let's just say that we want it to meet a certain specification. So we have a bunch of code. How do we know that the sum of user balances always equals the total supply? This is an ERC20 kind of standard exercise. Users cannot withdraw more than they deposited. People can't lose money. Only authorized parties, ACL, access control. So we have a bunch of code. How do we know that it actually satisfies these properties? So in many safety critical industries, by the way, including DeFi, formal verification techniques, formal methods are used to kind of prove software correctness. And I think that in DeFi, as TVL grows or sometimes it gets lower as this year, but as TVL grows and like we want to become more credible to the outside world or all other industries, we want to have very good techniques for showing that software is correct. And especially in an age of AI where replicating hacks, for example, last year, I'll talk about this a bit later, but after the balancer hack last year, YEETH was probably AI assisted, according to Twitter analysts, because they use similar kind of techniques exploiting some rounding issues. So in web3, the gold standard for like a protocol is like that it's been audited. And I'm oversimplifying maybe, but because people also use formal techniques, Certora is a very famous auditor that uses formal techniques. And the issue with kind of just reading code and reasoning about it is that you can find bugs, but you can't really prove their absence, which is kind of crazy. And also auditing is hard. Typically, you want to, if you're an auditor, you have a limited scope of time that you can actually work on it. So you, I know there's many auditors in the audience, so, so I want to be fair. But like you want to like look at the source code, right? Like you're not going to be thinking about the compiler. You're not going to really be reasoning about the bytecode usually. I mean, some people do if there's a really fancy audit, say, big deal protocols are going to pay auditors extra to like also verify the bytecode. But I mean, we want to kind of break it down into two parts, right? So we want to show that the source code satisfies the properties that you want. And we also want to show that the actual bytecode, which is that the actual artifact, the thing that's actually running on chain, also preserves those properties. So the compiler part I want to address. So as many of you may know, Viper, which I'm responsible for, had a compiler bug three years ago now, two and a half years. And that was in the non-reentrancy implementation. A lot of the funds were recovered, but not all. And so this is kind of a wake-up call to everybody that like, even if you like are positive, 100% that the source code is correct, the compiler can still have a bug. And that is kind of really what pushed us to look at more rigorous methods for like making sure that the compiler is correct. So that's why I'm here to talk about formally verifying the compiler and also formally verifying source code. So what we're doing is in the style of, I guess, KKML and Concert, which is like, we have an optimizing compiler and we actually want to prove that it preserves semantic correctness. So you have a definition interpreter. So you have a formal semantics of the language, which is like, you define what the language is supposed to mean. And then the compiler is the program that translates source code to bytecode. And you prove that at every step, every step of the compiler, going from source code to IR, optimizing the IR and going from IR to bytecode, each step is semantics preserving individually and transitively they compose into a semantics preserving function. And yeah, so as a detail, we're using the Verifarium formalization of EVM. So, and that is, I guess you can think of it, it's kind of simple, right? Because the EVM is simple. It does pass EAST, which is a fairly high bar. So you can think of that as like kind of a part of the trusted computing base, but which has also been extensively tested. And the reason it's part of the trusted computing base is because like the clients that are running the EVM on the network can maybe actually be running a different EVM. And you wouldn't really know that until you like hit some specific block. And this is kind of similar to a consensus failure, right? Like the clients in general can disagree on like what the EVM is. And actually I was curious if anybody knows, like what happens if there's a difference between EELs, like the execution specs and like probably the majority client go Ethereum. So if you know the answer, come find me afterwards because I'm very interested to find out what happens if they disagree. Okay, and the current status, here's a QR code for you to kind of investigate our progress. So the two previous compilers that I mentioned, Compsort and KKMLR for C and standard ML respectively. This is to our knowledge, the first formally verified compiler for smart contracts. And we have like completed the design and we're in the middle of the proofs. They're almost done, I promise. It's kind of like a lot of code and a lot of work, but we're making, I'd say very fast progress compared to what would have been possible before the age of AI. And I'll talk about that a little bit more later. But okay, now that we've talked about the verifying compiler, we're also interested in actually like, like, okay, verified compiler is very cool, right? You know that the compiler is correct, but like now you want to actually prove things about Viper programs, right? User programs. So here's a list of like common exploit vectors. It's by no means exhaustive. I'm not trying to pick on anybody, but like, there's just like millions of like failure modes, right? You know, like, and rounding errors are like a really interesting one because they're very difficult to reason about in general, both for humans and also for AI and also formally. So it's kind of an interesting case because oftentimes you have like a function that you know is like correct in the, like the, the continuous real valued form is correct. But once you start doing like losing precision due to division, integer division, then like all, it can be very unintuitive. Like you can like have a rounding that like errors that kind of compound and explode and then bad things happen. But that's just an example. Like we, our goal is to be able to prove like any useful and interesting thing that you want to know about a protocol's logic. So here's a kind of case study, snake mate. It's a third party library, kind of standard, standard used library for Viper. And we looked at the math library, which has a bunch of stuff in it, including muldiv, which is like 512 bit muldiv operation. It has transcendental functions, logarithm, the natural log exponential. It also has a cube root and the implementation is using a newtons-raphson iteration. And these are kind of interesting functions because we know how they behave in the continuous domain. But as I mentioned before, like rounding errors are very hard to reason about. It's an approximation. So you want to like be able to very, you want to be able to formally define or characterize how the approximation behaves with respect to the real function. And if like these functions don't behave like you want them to, then you can lose money. I hope the reason for that is clear, but basically it's like if you have some math function that like defines like how much two LPs are supposed to get in a trade or something.


And, like, the math function is wrong, then that can be exploited. And by wrong, I guess, doesn't match expectations. So, here's one of the functions. This is, what is this, the natural log, right? And this is like, yeah, this is cool, right? We've got all these constants. We have all these, like, weird, I mean, it's some kind of expansion. I don't know exactly what expansion it is. It's like not Taylor, it's something more fast or something. But, like, yeah, look at this. How do you know if it's correct? Like, raise your hand if you know. And the interesting thing is that it is. This was a bit surprising to me, which is that the error with respect to the real-valued natural log function is bounded by 3 halves of a watt. So, this is the theorem statement in Hall 4, which is the prover language that we're using. And you can actually prove this. It's quite a long proof, and it uses some very interesting techniques, but it's provable. This was the case for almost all of the functions in Snakemate that we found, actually, that they are, like, bounded very accurately by the function. Or, sorry, by some error, except for the exponential function. And the interesting thing about that is that it actually grows with the input. So, e of x for a very large x can diverge from the real-valued e of x by a constant factor, actually. And if you think about it, it makes sense, because, like, you just can't get enough precision in the domain in order to get precision in the output. So, here's another thing we're working on, which we're kind of excited to announce, which is that we're working with Curve Finance to do something more practical, which is kind of like, okay, so let's look at this function. Oops. Back. Okay. Back again. Whatever. But everybody, pretend they remember the natural log definition. It's kind of cool, right? It's a function that you can use, but, like, it's not really an end-to-end program. Like, it's a building block that you might use in your protocol. But, like, in real life, you want something that is about a real program. And so this is about the stable swap implementation, and what we're doing is proving that users can never lose funds. And this is a kind of, you can think of it as being exhaustive. So the theorem statement is that there exists no entry point into the contract where users can lose funds. And the way it's stated is in terms of virtual price. So if you're familiar with Curve code, that basically means, like, the user balances. So it means that, like, the users can never lose money via a rounding error, is how you can think of it. As everybody knows, you can, like, lose money in AMMs through impermanent loss, but that's not what we're characterizing. We're, like, characterizing that you can't lose money in an unexpected way. And this is actually, this is simplified a bit, but basically we're saying that, like, if you run, if you call the stable swap contract on the pre-state, and this is the post-state, then, like, the virtual price of the post-state is at least the virtual price of the pre-state, which is a succinct definition of users never lose funds. So we're very excited to finish proving this and announce the results. And I think this is, like, kind of everybody's, like, nightmare, right? Is the opposite of this. So you, like, put your funds in a protocol, it's been audited, but, like, you don't really know all the edge cases, so this is, like, kind of the contrapositive statement, which is, like, there is no edge case for which users lose funds. Okay. How much time do I have? Five minutes. Perfect. So this is a little bit extra for anybody who's kind of interested in theorem provers. SMT solvers are generally a little bit limited in expressivity. For example, they have trouble with for loops. The reason I wanted to look at these functions, the log x, cube root, moltiv, is because in SnakeMate there's some Hamos tests which are, like, not proving about boundedness, like the error bounds of the functions, but they're proving that the Viper implementation is equal to the Solidity implementation. And the solver that Hamos uses in this case, Yacis 2, is, like, it, like, can't solve it. It times out, runs out of memory. And so we're very happy to say that we're able to solve it. And this is kind of like a detail, but there's other theorem provers, Lean, it's now renamed to Rock, Isabel, LCF architecture for, if that means anything to anybody. It does to me and maybe some other, like, nerds. But they're conceptually all kind of similar. They have some higher order logic and they're expressive enough that you can prove kind of, I would say, like anything useful that you want to say about software. And the differences are mainly in tooling, the community. We have actually been submitting some stuff upstream to Hall 4, which makes the experience better for agents. So this ties into the context of, like, we're in the age of AI, right? AI-assisted hacks are getting better. AI is writing more code. How do we verify all the AI code? And what we need to do is basically boil down all of software to the things that you need to know, which is, what is the software supposed to do and how do I know it's correct? And that happens when you have well-specified contracts and you have a theorem prover. And actually, we're working on a prover agent which will do these proofs for you so that all you need to do is think about, like, what does it mean for my software to be correct? And then the end goal is you push a button and then you know it's correct. And we believe that this scales to other languages, not just Viper. These are some kind of, like, well-known high-assurance languages, C, COBOL, and Ada. So I want to go to the last page first, which is cut off. Okay, so those are our socials. The other page, you can book a call with us or reach out to us on Telegram. And I'm happy to take questions. How do you go from the theorem to the actual proof? So the question is, how do you go from the theorem to the actual proof? Well, you ask the prover agent to prove it. Is that your question? Like, how do you actually produce the proof? Yeah. I can describe the architecture of it. So generally, what you want is, like, an easy-to-understand model of your code. So, for example, the snake mate properties that we're proving are things like, it approximates the real-valued function. And the real-valued function is something that can be expressed in Hall 4, which is kind of crazy because, like, some of these, like, real numbers are, like, not actually representable. But, you know, it's like a Taylor expansion and it has a mathematical representation. So you have that model, which is, like, some Hall 4 function, and then you need to prove that it's equivalent to the source code. And basically, you need to prove that it's called simulation. So if you run the program through the interpreter simulation, then it matches the pure function that you've defined. And the correctness properties can also be, like, this function is monotone. Or, like, this function only ever touches these storage slots. And then, yeah. And then the proof propagates to the bytecode by the compiler verification proof. Okay, thank you. A nice talk. So I'm just wondering, how much time have you actually spent building the verified compiler? And could you extrapolate that to how long it might take to build a verified solidity compiler? I'm not really going to comment on building a solidity compiler because I'm not familiar with that compiler. It took about, let's say, a year to define the formal semantics for Viper. And then since December, work has been progressing much more quickly. Yeah. CompCert took 15 years, so we really are in a new era.


Can I ask how low level you've done the verification for? Is this just machine code or do you go down to register's transfer level or even lower? So we prove equivalence with the EVM bytecode. So we have, like I mentioned earlier, there's the EVM model, which is VeriFurium, which is an EVM implementation you can give it us. It passes the EAST. So the proof is that the bytecode semantics is equivalent to the Vyper semantics as we've defined it formally. Does that answer your question? And that includes all the optimizations, so including lowering to like EVM operations, memory, stack spilling, and all this stuff. Yeah, so it includes correctness of the stack spilling algorithm, all of the optimizations, control flow stuff. Semantics preserving. Okay, I'm around all day. Feel free to come talk to me. Oh, I didn't put my Telegram here or Twitter, but it's big underscore tech underscore SUX, sucks.

