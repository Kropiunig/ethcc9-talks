---
title: "An Offensive Security View to Cross Chain Bridge Security"
speaker: "Ece Orsel"
organization: "Hacken"
stage: "Taylor"
youtube: "https://www.youtube.com/watch?v=d1SKvFkelLQ"
---

# An Offensive Security View to Cross Chain Bridge Security
**Ece Orsel** (Hacken) --- Taylor Stage

Can you hear me? Yeah, great. Yeah, today's topic is offensive security with cross-chain bridge security. But before I introduce myself, I want to talk about numbers. Since 2022, more than $2.5 billion have been stolen from the cross-chain bridges, not from DeFi, not from wallets, directly from the bridges. And you can think that those cross-bridges are the bank of Web3 ecosystem. And they run with the mixed code, servers, and humans. And the most interesting part is the most bridge hacks were not caused by the smart contract vulnerabilities. It's happened somewhere else. In APIs, in the validator operators, in the network infrastructure, in what we call Web 2.5 area. It's a gray zone between where Web 2 meets Web 3. And I'm Ece. I have worked in Hacken since three years now. And I have been working in this area for about nine years. And in my daily work, I test the system that I mentioned before that stays in the gray zone area, like the mobile applications, web applications, browser extensions, desktop applications, source code analysis, including the gaming system. And at Hacken, we also conduct smart contract audits, Layer 1 security, including we have Bug Bounty programs. And here's what we will cover today. We will start with basics, like how bridge actually works and what's inside it, why architecture matters, and what each one means for the security. And here, there's a trick question we should ask. First one is the how does a bridge work? The other one is what's the off-chain layer and what's the Web 2.5? Let's imagine two isolated islands. And left side is the Bitcoin and right side is the Ethereum. And each one has its own rules, own protocols. They don't communicate with each other. They don't interact with each other. There is no native connection between them, right? So the question is, how do you move your assets? How do you move your value one to another? So that's where bridge came in. Most of the common mechanism is lock and mint. Say you want to use your Bitcoin on the Ethereum island. You sent your Bitcoin to the bridge and bridge locks it and no one can touch it. And you can use your Bitcoin as the wrapped coin, wrapped token in the Ethereum island, just like any other user. And when you want to go back, you do it in reverse. You wrapped your Bitcoin and it gets automatically burned. The lock side opens and your Bitcoin is back. Now, look at the bottom of the picture. Who actually approves the lock? Who actually approves the burn? Who decides that the message coming from one chain is valid before the another chain acts on it? You know? It's the validators, it's the API, the network infrastructure, the private key. So that's why we call Web 2.5, not fully decentralized like Web 3. And it's not traditional Web 2. It's somewhere between them and they're carrying the risk from one world to another. And this brings us to the most important point of the section. Because the weakest point is the validators. They live in the Web 2.5 area. They can be manipulated, they can be tricked, they can be taken down. So they can be compromised. And every major hacks we have seen so far, like Ronin, like Harmony Horizon, didn't happen on-chain side. The on-chain side was fine, but the off-chain layer was broke. But not every bridge works the same way. Look at the table. There are four different models and there are four different trust assumptions. First one is canonical. It trusts the chain itself. Minimal 2.5 attack surface, but the speed is really slow. The other one is the validator. It trusts human and keys. And minimal Web 2... Sorry, it's high Web 2.5 attack surface, but it's also very fast. The other one is the proof-based. It trusts mathematics and the ZK proof. Low 2.5 attack surface, but it's really slow and expensive. By the way, the industry is moving here, but not just fast enough. The other one is the liquidity. It trusts relayer and attack surface is medium to high and it's really fast and user-friendly. So at the end of the day, the pattern is clear. Three out of four models have Web 2.5 exposure. Proof-based is still evolving, but until that changes, billions of dollars keep flowing through the off-chain sites. Now, let's see what happens when attacker actually find it. I believe you already know that the Ronin attack is conducted by the Lazarus Group. They spread phishing attack, developer targeted via fake job offers, and the private key extracted from the local machines. And as a result, $625 billion has gone and the chain never knew that. The other one is the Harmony Horizon. Same result, but different path. Server infrastructure compromised directly and they only need two keys and $100 million has gone. The other one is Shelly Halud. Actually, this one is the most smartest hack I have ever seen so far. It's a self-spreading form and the story behind that, the attackers hacked the NPM validators and then they upload their backdoors to the trusted packages. So as a developer, when you run the NPM command, it's automatically the worm in your machine. So you were already hacked. And the interesting part is, it's affected almost three layers, like Web3 area, Web2 area, Web 2.5 area. In Web2 site, cloud server was compromised and in Web3 site, the validator keys, the environment files, the credentials was taken from the developer's local machine. And the Web3 site, the attackers used the stolen credentials and they upload the fake version of the trusted Chrome extensions and users who logged in, their wallet data was stolen silently. And it nearly affected 20 million users right now and the effect is still going on. Like I heard yesterday that two Axios versions was affected from them. The fixes come the same day, but if you are using the older versions, just be careful about and aware of it. And let's map the attack surface systemically. There are five issues here and each one is positioning on the off-chain side, but they are also affected on-chain side directly. First issue is the cross-chain message spoofing. In here, every chain has a fundamental question that needs to be answered. The question is, did the lock actually happen on the other chain? Because an attacker sends a crafted bridge messages and the chain accepts these assets without a wallet transaction on source chain site. And look at the attack flow. Attackers submit the crafted bridge messages, backend accepted this message as valid. And in transaction, there is no cryptographic commitment right to this message.


message to a real on-chain event, so there is no signature verification. And as a result, mint executed on and you gave unauthorized assets. This is the basic requests you can see. These are our main playground, endpoint, headers, and the functions. And look at the TXHash. It's fabricated, nothing was ever logged in the Ethereum site, and there is a signature verification, but the field exists, but it never checks what's inside it. So as a result, 1 million wrapped token minted for nothing, just the message here said that Trust me. And the other issue, sorry, and here's something worth pausing on. Let's say smart contract is perfectly written and the signature verification is solid. Does it mean you are safe? No. The answer is no, because the attacker doesn't have to go through the legitimate flow. They go directly through the API, they craft the message, and the API passes to the relayer, and relayer passes the smart contract, and the smart contract assumes that this message is valid and the signature is valid, and you automatically got approved. So the main issue, main problem is that there is no defense in depth mechanism, so every layer needs its own control. So smart contract doing its job doesn't mean that the API layer is doing its job. So Web 2.5 is just the additional attack surface, so you can bypass on the on-chain layer. But the other main problem is here, there is a disconnect between the on-chain and off-chain site. So the teams working on these parts need to be more aligned, more aware, like what each site is doing. And it's really easy to prevent, but that's why this kind of issue becomes more possible today. The other one is the replay attack. Replay attack is basically when someone takes a valid transaction and just sends it over and over again. The system keeps paying out every time, and it happens because the bridge doesn't track what's already proceed. So it treats the same request, it's like a new each time, let's say. So let's walk through this. This is a simple request and response. The real claim exists, the attacker doesn't create a fake one, just duplicate the same request, same TXHash, same signature, same amount, same amount field can be changed, by the way, and sends it again, and the bridge gets it and checks the signature is valid, checks the amount is valid, but it never asks, did I already proceed this before? So there is no nonce here, there's no tracking in here, so it pays out again and again, same transaction and second payout and third payout. The fix is actually really simple in here. Every claim should be tied to a unique identifier like the nonce, like TXHash, like the registry or the message ID. So in short, replay protection must be enforced to the bridge logic level. So this one is different from everything we have seen so far. Let's say you logged in some of the decks platform, and each platforms gives you, assigns you an ID. And imagine your user ID is 1. When you leave your own profile, you can able to see your personal details like the user ID, username, QIC status, or the sub accounts. But the problem is here, when you change your user ID 1 to 2, you can able to observe the other's user's data. And it means that you can effectively enumerate all the user's data stored in the system. So you have on-read access, but this is what we call as the vertical IDO scenarios. And there's also horizontal scenarios, but in horizontal scenarios, you can able to modify the other user's data. The other issue is the unauthorized universal transfer. Let's say the attacker is using their own wallet session, so the authorization header is completely belongs to the attacker. However, in the transfer request, they have set the from account field to the victim ID, victim's wallet address. And as a result, they simply reuse the wallet address they were able to observe in the IDOR issues. The core misconfiguration here, the system only checks the whether the wallet is exist, but never checks, never verifies the who actually belongs to, because of that you can change the wallet address here, you can take the funds, others, users' wallets. So the authentication and the authorization are two different things. Authentication answers who are you and authorization answers what are you allowed to do. In this scenario, the authorization header is valid, but it never asks the who belongs that authorization header. And the other one is the social engineering attack. This attack is become very popular lately. The attacker's group reach out to developers on LinkedIn via fake job offers. And the conversation feels natural, the interviews and technical talks, everything looks professional. But then comes the technical challenge, they send you a coding test, but hidden inside the coding test is a backdoor. So while you are working on this challenge, the attacker already has access to your machine and quite stealing everything in your local. And what makes this especially dangerous, it's how they target it. First, they're profiling the persons, users, and they build the whole approach around that one person. And like whoever the most likely have the validator keys on their machine, they target that person. And once you download the task locally and start solving the challenge, you got automatically hacked. And last one is the, it's mostly related to Web2 infrastructure, but it's directly affected Web3 area. On July 2024, most of the several DeFi protocols were affected with DNS hijacking attacks. The bridge code was clean, there is no issue in the on-chain and off-chain logic. However, the attackers came from the off-chain site. The domain registrar account has no factor authentication. So the attackers runs a simple DNS lookup command. And with one command, they can able to observe the provider, region, namespace, register, and there's no DNSSEC in here. And the attacker gets into registrar account, like credential stuffing, brute forcing, or two-factor authentication bypass. It depends on the system and the attacker's mind. And they changed the DNS record. And at the end of the day, the site looks identical, same logo, same UI, same colors, but the approve button calls the smart contract's attacker's smart contract. So when you click the approve button and you confirm, you just gave you an access to the attacker. So it means that your all tokens gone forever, the attacker drains able to all the user's wallet in the system. So thank you for listening to me today. We can take questions. I think that's all. Thank you. Thank you again.




