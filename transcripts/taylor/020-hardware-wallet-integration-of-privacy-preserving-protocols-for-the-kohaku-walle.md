---
title: "Hardware wallet integration of Privacy preserving protocols for the Kohaku wallet"
speaker: "Reno"
organization: "ZKNOX"
stage: "Taylor"
youtube: "https://www.youtube.com/watch?v=f-i4i9oNluA"
---

# Hardware wallet integration of Privacy preserving protocols for the Kohaku wallet
**Reno** (ZKNOX) --- Taylor Stage

Hi, I'm Reno from Zicknux. Today we're going to talk about the work we've done inside the Kwaku framework, so hardware wallet integration of privacy-preserving protocols. So first, the Zicknux team, we are a huge team of three people, but while small, we are covering the whole field, being from user-end, so secure enclave, hardware wallets, that's just the topic today, backend, and on-chain. So yeah, that's the three of us. So Kwaku is a privacy-first tool for the Ethereum ecosystem. It's an SDK. It's live. The aim is to provide a better UX for privacy, which has been identified as one of the limitations for a wider adoption. And in this framework, what Zicknux is providing are some experimental features, mainly hardware integration, that's the subject, peculiar experimentation. We also co-authored two EIPs. Tomorrow, Simon will present those results. And we also provided a 7702 jailbreaker to enable to experiment with ledger that is breaking to whitelist. And everything is available in our GitHub. So yeah, what was the difficulty? So the hardware wallet gap. So Kwaku wraps privacy pools and Regan, and hardware wallets do not support Poseidon or BabyJubJub for those fields natively. So privacy pools, both and Regan both use Poseidon over the BN254 fields. And for Regan, there is BabyJubJub curve to be implemented, which is not part of common SDK for hardware wallets. So first, I would like to have a brief talk about the fact that indeed, math are easy and encodings are hard. So I don't know if you have some implementers here. And in general, we spend more time solving encoding problems than implementing the elliptic curve itself, double encoding, cannot just say, Hey, LGTM and push it. So it's also an interesting thing to have a double encoding of protocol because that's what we do. Our stack has no external dependency. So some kind of bugs that we found during the project was like Lagrange interpolation bug. There was a division that was happening in natural integers instead of the fields. And I think that you cannot have missed last week. So privacy pool did the job, but you had to migrate from the first derivation. There was an improper derivation that crashed the entropy. There is a full article describing everything. So everything has been patched and fixed now, but it would have been solved while implementing this. But that was not in our scope. But yeah, so basically double encoding is like having a free audit. So back to our topic. So Regan uses BabyJubJub, which is a twisted Edward curve. And yeah, mainly it was quite easy to implement for the hardware constraints. The challenging was the Poseidon part. Poseidon, for those who have used circle lid, it handles a large set of tables, and this does not fit into constrained hardware. So what we had to do was to regenerate the constant on the fly, use tricks such as having starting point in the derivation. So I know that there are initiatives to renew Poseidon. As an embedded implementers, I would ask EF to consider to use not the Cauchy construction, which implies inversions, and is a nightmare, and replace it by maybe like circular and Van der Waals matrices. But doing this, we were able to have it fit in the hardware. So what is Regan? Regan basically is a Zcash-like mechanism. So an interesting property of it is that you can separate the witness generation from the proving, which is not the case for TornadoCache, where your nullifier, which are secret elements, has to leave the enclave at some point to be proven. So this is a very interesting property, providing the same security we have with, I guess, the rest of your classical transaction, where you don't want any malware to be able to steal funds. And I guess that's providing this hardware integration will also help increasing the TBL because myself, I never use a software wallet to store more like a few hundred dollars, and you shouldn't. So this is a Regan circuits. Yeah, so I guess I won't go deep into details, but basically what happens, you have a circuit proof, so the ownership of the notes after receiving from the signer the signing of the UTXO. So yeah, so as I said, TornadoCache privacy pool v1 do not meet this model. We had discussion with privacy pools. They are switching to a UTXO model. Yeah, so another thing where we have been working on is Frost. So Frost is a K out of 1 threshold signature scheme. So it enables to have K signers to produce a valid signature. It's like what you are doing on your safe, I guess, when you have a governance. So you accept a signature valid as soon as K signer from a subset of K signer from an agree to sign on the message. Basically, it's based on Lagrange interpolation. Each signer holds only one of the shared secrets of the key, and the key itself is never reconstructed. So Frost works for schnorr curves, like implemented in Taproot, Taproot to schnorr, and by chance, EDSA, which is what is implemented in circle lid, is the schnorr version, which is mandatory to be able to implement Frost. That is very simple compared to ECDSA constructions. Yeah, so as a result, compared to naive code of and signing, it provides private governance because looking at what happens on chain, you have no clue if the Frost has been used or if it's just a monosigner. And yeah, it comes with no overhead. So basic idea behind this. It's using Lagrange interpolation. It's like a mechanism that is used in your CD-ROM to protect from erasure. It's, yeah, as soon as you have k value from a degree T minus one polynomial, you can reconstruct the polynomial. So this is the idea behind this. So it uses the linearity we have in schnorr. So if you look at the formula of the schnorr generation, R is the nonce. SK is the secret key. The produced signature is linear in the secret key. So basically, you replace the secret key by those interpolated points. So one ancestor was Mosick2, which is used in Lightning and Ionic. Yeah, so for completeness of the slides, here is the full description. And important thing, we used VSS for the DKG. It means that the initial generation of the shared key. There is no trusted dealers, meaning that any participant once, now, you have this MPC way to generate the initial key. So the idea behind this is not to rely on a single, for instance, a single hardware signer. For now, we did the work for Ledger. In the future, this morning, Trezor presented their move into a platform. We could have this in Trezor. And this way, if at some point there is some supply chain attack or backdooring by one of the provider, you will still have the security of what is left. Yeah, so for completeness on later, this is the whole protocol. This is how it has been implemented in the Ledger, the state matching. Yeah, so that was for Ledger. Soon for Trezor. But doing all of this, so we have...


We have this working, we know we have the blind signer. One thing is when you want to have clear signing here, you have a larger constraint that is signing all the inputs. And if you look at existing privacy signers, for instance, Zcash or Namada, clear signing is not provided, mainly because Poseidon is expensive. And one way to do this could be to instead of computing all the chain of hash inside the hardware wallet, what we have been doing is implement a verifier, a ZK verifier inside the hardware wallet. And once we have this, this means that basically your little hardware wallet has unlimited power computation for all public inputs. So this has been done. This can also solve all the clear signing trust issues that we could have, because right now the 7730 initiative is discussed. The solution, as it is now, would be to implement tunnels between the dApps and the hardware wallets. This is good, but here again, math is easy, but key management is hard. I guess we have like this resolve incident, so we know that we will have still beautiful disasters. So what we could do would be to have the clear signing itself being computed outside the hardware wallet, having the verifying key inside the contract, so the hardware wallet can compute the proof of storage of the circuits and then the statement, this human-readable string, correspond to this code data, could be the ZK statement to be verified by the hardware wallet. It means that as long as you are able to prove it, you can have anything being outsourced, all public inputs can be outsourced of the wallet. So we think this is a huge improvement compared to what we have now and the limitation the constraint hardware implies. Yeah, so to conclude what has been done during those past eight months inside Coacoo. So we provided the first TSS plus ZK signer complying to RFC9591 on BabyJubJub. We provided the first ZK verifier running fully on a secure element. Runtime is 2.8 seconds, so in terms of UX, it's quite acceptable. If you have ever used either Zcash or StockNet hardware integration, yeah, verifying times are very long right now. So we hope to have collaborations in the future to integrate this kind of mechanism for a wider use outside of Coacoo. Yeah, so the next steps, generalize the ZK clear signing to maybe more functionalities. This was the obvious first application case. Explore Stark implementation in hardware because all of this use Groth16, we will have the Plonk verifier very soon, but it's not post-quantum resistant. Simon will present the stack tomorrow, but yeah, we began to see will it be possible to streamline the proof generation. That might be not sure it's feasible, but that's something that we are exploring. And there is also another password that we are exploring today. So right now there is only Ledger platforms that will be tradable, but it would be good to have a universal VM, meaning that you don't want to redevelop a new application for every provider. And this is something that the chip will present in two days. So if you're interested in integration, exploration, get in touch with us. Okay, and I guess that concludes the talk. If you have any questions. Do you have any questions? Thank you.

